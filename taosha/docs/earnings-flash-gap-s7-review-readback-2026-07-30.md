# exp17 `earnings_flash_gap` · §7 复核 B 项只读补证

日期：2026-07-30（UTC+8）

Fable 对 §7 正式运行给出 `A0/B2/C1`，运行通过；两项 B 级仅要求在 persist 前补足
既有 result 原件的外部可核性。本次只读消费
`/root/s17run/delivery/result_exp17.json`，不重跑研究、不改 result/report/log 原件、不写数据库、
不 persist。

## B1 · 主窗样本差 19 的构成

正式 result 的 `per_tau.by_tau` 在主窗 τ=0..4 的样本量为：

| τ | N | 相对 N_valid=1,841 的缺失数 |
|---:|---:|---:|
| 0 | 1,841 | 0 |
| 1 | 1,837 | 4 |
| 2 | 1,835 | 6 |
| 3 | 1,831 | 10 |
| 4 | 1,829 | 12 |

主窗完整样本 `N=1,822`，故至少缺任一主窗 τ 的事件并集为
`1,841−1,822=19`。逐 τ 缺失并集必落在
`[max(0,4,6,10,12), sum(0,4,6,10,12)] = [12,32]`，实测 19 在界内；
逐 τ 缺失事件存在重叠，因此并集 19 小于逐 τ 缺失数之和 32。守恒检查通过。

## B2 · N_eff 全精度与分母

正式 result 原值与按引擎公式复算逐位相等：

- 诊断分母：顶层 `N_valid=1,841`；
- `rho_bar=0.009815666688264883`；
- Kish：`96.58552739379029`；
- KP：`95.63747604998257`；
- Kish 公式：`N / (1 + (N−1)×rho_bar)`；
- KP 公式：`N×(1−rho_bar) / (1 + (N−1)×rho_bar)`。

`n_eff_rho` 仍是 `NOT_FOR_VERDICT` 诊断；主窗 ADJ-BMP 继续使用主窗完整样本
`N=1,822`，两者没有混用。

## 原件与取证包

- result 原件 SHA256 在补证前后均为
  `a5249ec4554aff2476bc33b33b2e2600dd7cdcba01a0c98fb04e27a382394db7`；
- 补证 JSON：`/root/s17run/b_followup_readback.json`，SHA256=
  `386759c2fec7a6a5a7bdff9f365a8eae6c522a81e828a7aad32d4278481824c2`；
- 取证包现为 30 项，更新后的 `SHA256SUMS -c` 全部通过；
- 13 类秘密扫描 `TOTAL_HITS=0`。

两项 B 级据此闭合。exp17 仍停在取证点；密封原文“正，把握度80%”未开封，
校准册未入册。下一步须经复核后由 John 另行授权 persist。
