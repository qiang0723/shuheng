# exp22 `delist_warning_financial` · 冻结前只读准确性窄闸令

- 日期：2026-08-09（UTC+8 / Asia/Shanghai）
- 性质：既有排期的下一条只读准确性核查，不构成 PAP、冻结、运行或数据建设授权

## 一、人令原文与唯一解释

John 原文：

> 继续按照计划开发吧

当前权威台账中，exp18 `audit_qualified`、exp21 `goodwill_impair`、exp23 `buyback_announce` 均停在既有证据语义硬门；exp25 继续封存，exp9 仍有既知数据前置缺口。exp22 是 registered 池内唯一未被既有停止线封住、且与当前公司公告类轮巡一致的候选。因此本令把“按照计划”限缩解释为：只推进 exp22 的冻结前只读准确性窄闸，不恢复其他停止项，不启动 Web 新功能或票池工程。

## 二、核查目标

1. 只读确认 exp22 身份、trial、状态、PAP/result/done 槽、addendum、StudySnapshot 与台账基线。
2. 核查“因连续亏损、净资产为负或其他财务退市风险原因而实施退市风险警示”能否由当前在库 PIT 数据确定识别；不得以 `*ST` 名称字面直接冒充财务原因。
3. 核查候选事件锚：名称变更 `ann_date/start_date`、财务报告首次披露日、审计意见披露日之间的时序和唯一性；后验年报、后续更正或实施状态不得回填较早事件。
4. 评估新落地的 balancesheet PIT、fina_audit 事实及既有 forecast/income/namechange 数据是否足以形成可重复、fail-closed 的原因规则；逐项披露字段、批次、时间轴、版本与污染边界。
5. 在可证口径下报告候选漏斗、逐年分布、财务原因构成、无法判定量、selection SHA；不能精确识别时只允许报告代理上界并明确 NOT_FOR_VERDICT。
6. 复核 exp22 与已闭卷 exp568 的事件键/证券重叠、同族 trial 顺序与族内 alpha 口径；不得因 exp568 已显著而放宽 exp22 规则或择样。
7. 核查现有单事件集 `adj_bmp_main_only` 路径、三窗口、身份水印与 fail-closed 模式能否复用；需要新数据工程、OCR/大规模人工、通用原因分类能力或新统计轴即列为阻塞，不顺手建设。

## 三、执行边界

- 允许：仓内代码/DDL/历史取证只读审计，数据库 `BEGIN READ ONLY` 查询，已有事实与视图的只读计数、集合对账和小量样例核查；
- 禁止：新增采集、缓存、落库、视图/DDL施工、生产代码、PAP、冻结、StudySnapshot、研究 manifest、收益读取、正式运行、persist；
- 禁止使用标题关键词、`*ST` 字面、后验年报值、后续实施/撤销状态或人工猜测替代财务原因；
- 任何精确原因或首次披露语义不能证明，结论必须为“当前不可冻结”，不得自动缩短研究期、改登记命题或寻找代理自救。

## 四、交付与停止线

交付一份自足窄闸报告，至少包含：状态基线、数据资产矩阵、原因与锚规则可操作性、候选/污染守恒、与 exp568 重叠、判决路径复用、A/B/C 分级、证据边界及“可进入 NOT-FROZEN PAP 草案 / 当前不可进入草案”二元结论。完成即停交验点，下一步须 John 另令。
