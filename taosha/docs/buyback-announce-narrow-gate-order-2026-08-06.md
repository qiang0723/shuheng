# exp23 `buyback_announce` · 冻结前只读准确性窄闸令

日期：2026-08-06（UTC+8 / Asia/Shanghai）

> John 授权原文：
>
> 批准 exp23 buyback_announce 进入冻结前只读准确性窄闸，按上述边界执行。

上述边界逐字承接为：只核回购数据实物、预案首次公告日、进展/完成/终止污染、多版本
处理、注销式与库存式能否区分，以及既有判决路径能否复用；不采集全量、不落库、不写
生产代码、不做 PAP、不读收益。exp18、exp21及其他候选不并行。

## 一、只读基线

1. exp23 必须为 `registered`，`frozen_at/result_json/done_at`均空；
2. 核对台账分布、exp23 addendum、StudySnapshot/manifest、正式产物与遗留进程；
3. 盘点 qbase/taosha 现有回购事实批次、表、视图及可用字段，只读不补抓。

## 二、数据与事件语义

1. 核验 Tushare 官方回购接口字段、参数、历史覆盖与阶段枚举；
2. 限量探针只准使用官方文档、最多30票及最多5个小日期窗；仅用于字段和多版本形态，
   禁止据此外推全市场事件频率；响应不落库、不缓存入仓，报告完成后清理临时响应；
3. 识别预案、股东大会通过、实施、完成、终止等阶段是否可被结构化字段严格区分；
4. 核验 `ann_date`能否忠实作为首次预案公告锚；多行、修订、同日冲突与缺失如何
   fail-closed，不得以实施日期或后续阶段回填；
5. 核验注销式与库存式是否有可靠结构化字段；不可区分即如实列为冻结前缺口，不用标题
   关键词或后验实施结果代理。

## 三、判决形态与二元结论

1. 只核现有单事件集、三窗口、market benchmark、ADJ-BMP唯一判决路径能否复用；
2. 注销式/库存式只能先评估为组成审计或冻结前语义项，不读取任何事件后收益；
3. 输出二元结论：`可进入NOT-FROZEN PAP草案`或`暂不可进入`，并列最小真实阻塞项；
4. 不提出平台扩建、阈值优化、代理事件或敏感性分析。

## 四、停止线

本单元零全量采集、零缓存入仓、零数据库写入、零生产代码、零 PAP、零冻结、零研究
manifest、零收益读取、零正式运行、零 persist。完成报告、更新 STATE 后停交验点。
