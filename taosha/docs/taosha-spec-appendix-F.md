# 淘沙施工 spec v0.2 · 附录 F(#2b 事件定义落地附则,2026-07-08 人批冻结;含 rev1 修订)

> **与 spec 正文同等效力(附录 C 先例)。** #2b = `drawdown_rebuy#2` = 台账 `exp_id=3`(frozen)。
> 本附则冻结 `event_def`(§6 逐字继承、仅池改 b1)的操作化九点 + #7 补丁 + 强制诊断。
> **裁决过程在见数之前(数据盲不破)**:架构窗口原倾向"上穿 / 加超时"经论证撤回,采工程建议读法;
> 工程侧迄今零出数(未做任何事件生成或出数对比,声明在案)。附录 F 成文 commit 后事件生成方放行。

---

## F-rev1 修订(2026-07-08 人批,C 案主体;**显式作废 rev0 对应条目**,F 条改判纪律)

**改判缘由**:rev0 的 **latch 语义**(F1.1 首触即 armed 并 latch)+ **#7 补丁失效后锁区**(F1.5「须新一轮
回撤方重新 armed」+ F1.7「进场**或失效**后须先脱离回撤区」),经工程侧数据盲下自检发现产生**死锁**——
armed 于回撤触发(下跌途中 ma20 仍高)→ 次日 close<ma20 立即失效 → #7 锁区,而反抽在回撤区内不脱离
→ **常规回撤反抽形态 0 进场**。裁定:latch 与 #7-失效锁区**系架构窗口操作化时引入、非 pap 原义**,
**C 案系向原文「回撤后等反抽」回归**。死锁发现记功;**数据盲不破确认**(纯逻辑构造中发现,零真实数据、
零出数);效力 **full 不变**。

**rev1 五条(以下 rev1 版为准;rev0 对应条目作废):**

1. **armed = standing 条件**(作废 rev0「首触即 latch」):当日 `close ≤ 0.9×H60` 即在回撤区,**逐日重判**,
   非首触 latch。离开回撤区(`close > 0.9×H60`)= 本轮 episode 结束;再入 = 新 episode。
2. **站上 + 未破 ma20**:`close > ma10`(水平,等号归破)**且** `close ≥ ma20`(`close < ma20` = 破 20 日线)。
3. **失效改计数清零、不锁区**(作废 rev0 F1.5「失效=解除 armed+须新一轮回撤」与 F1.7「失效后须脱离」):
   在回撤区内破 ma10(`close ≤ ma10`)或破 ma20(`close < ma20`)→ **仅计数清零**,**不锁区、不解 episode**;
   仍在回撤区、后续满足即可再累计。连续 3 日「站上 ma10 且未破 ma20」→ 第 3 日 = 进场 T。
4. **#7 防链式移至进场后**(作废 rev0「进场**或失效**后」中的「失效」):**仅进场后**须先 `close > 0.9×H60`
   (脱离回撤区)方可再产新进场——防同一波连发链式进场;失效不触发此锁。
5. **新增诊断 D3**:进场前自 episode 起曾 `close < ma20`(破 20 日线致清零)的事件占比——rev1 放开锁死后的
   **成色自曝**(= rev0 会锁死、rev1 仍能进场的集)。报告项,不进 verdict、不作筛选。

**不动条目(rev0 仍有效)**:#3 站上=水平判据(rev1 沿用)、#6 armed 无超时(standing 天然无超时)、
#8 事件锚 T=进场日 τ=0:=T+1、#9 PIT 强制、H60/均线口径(F1.1/F1.2)、D1/D2 诊断(F2)。

**固化回归用例**(自检 `python -m taosha.compute.drawdown_signal`):R1 死锁序列(rev0 产 0、rev1 须进场)、
R2 链式防护(进场后未脱离回撤区仅 1 进场)、R3 脱离后再回撤可第二进场、R4 失效不锁区 + D3。**自检绿后
事件生成放行**(状态机 `drawdown_signal.py` audit `5e713974`)。

**污染标注(效力 full)**:rev1 改判 = 死锁修复向 pap 原义回归,LLM 提出、人批冻结、数据盲不破;
随本 rev1 追记 `exp_id=3` contamination_note。

---

## 〔以下 F1–F4 为 rev0 存档;经 rev1 改判的条目(F1.1 latch / F1.5 锁+新一轮 / F1.7 失效后#7)已作废,读法以上 rev1 为准〕

**事件定义原文(台账 exp_id=3 pap_json `event_def`,§6 逐字继承、仅池改 b1):**
> 「b1 池(PIT)内收盘自 60 日高点回撤≥10% 后,站上 10 日线且连续 3 日不破 = 进场;破 20 日线 = 失效。」

---

## F1 · 逐条冻结(九点)

1. **H60 与回撤触发**:H60 = 以评估日为末端的 **trailing-60 交易日后复权收盘最高价**(逐日滚动);
   回撤触发 ⇔ `close ≤ 0.9×H60`。

2. **均线**:ma10 / ma20 = **trailing-10 / trailing-20 交易日后复权收盘 SMA**(非 EMA)。

3. **站上(水平判据)**:`close > ma10`;armed 后首个满足(`close > ma10`)之日起算连续计数。

4. **连续 3 日**:连续 3 个交易日 `close > ma10`,**第 3 日 = 进场日 T**;期间任一日 `close ≤ ma10`
   计数归零(armed 内重计)。**等号归"破"**(`close = ma10` 视为不满足 `close > ma10`,即破)。

5. **破 20 日线失效**:armed 期内任一日 `close < ma20` → **失效** = 解除 armed + 计数清零,
   须新一轮回撤方可重新 armed。**效力域限进场前**;进场后的"破 20 日线"属**策略版出场规则
   (附录 B 域)**,状态机分开;**事件版检验窗按 pap 20/60 固定窗,不受出场影响**。

6. **armed 无超时**:终止仅由"进场"或"失效"二者先到(**不引入自由参数 / 时窗**)。

7. **同票多次进场(逐次独立事件)+ #7 补丁**:允许同票多次进场。
   **"新一轮"操作化(补丁)**:进场或失效之后,须**至少存在一日 `close > 0.9×H60`(脱离回撤区)**,
   此后再度 `close ≤ 0.9×H60` 方构成新一轮 armed。理由 = 防"进场点仍在回撤区内导致 armed 立即
   重挂"的链式病灶;H60 滚动使"脱离"自然可达,**无新参数**。

8. **事件锚**:T = 进场日(收盘确认);**τ=0 := T+1**(S2-DEC3 τ 轴,盘后信号前视规避)。

9. **PIT 强制**:全部指标(H60 / ma10 / ma20)**仅用评估日及之前数据**,无前视。

---

## F2 · 两条强制诊断(报告项;**不进 verdict、不作筛选**)

- **D1**:进场前自 armed 起**从未跌破 ma10** 的事件占比(= 水平判据 vs 上穿判据两读法的**分歧集大小**;
  只报不筛)。
- **D2**:**armed→进场天数分布及长尾占比**(`>60` / `>120` 交易日)——armed 无超时的成色由此自曝。

---

## F3 · 状态机与策略版边界

事件版状态机(F1)**止于"进场"**(生成事件锚 T);进场后的持仓 / 出场(成本 −20% 强平 或 收盘破 20 日线,
先到先出)属**策略版**(附录 B:单事件持有路径模拟),二者分开。事件版检验用 **pap 固定窗 20/60**,
策略版按离场;同一事件同时输出事件版与策略版两份结果(附录 B B2)。

---

## F4 · 污染标注(效力 full)

九点读法 + #7 补丁 = **LLM 拟定、人批冻结、数据盲**(零出数声明在案);未触样本收益数据;**效力 full**。
记入 `exp_id=3` contamination_note(随本附则追记)。

---

**放行条件**:附录 F 成文入档 + commit 后,事件生成放行 → 建 PIT 状态机(F1)→ 接引擎(基准 = b1 池等权 PIT、
窗 20/60、流水线沿用 #4:删失诊断 / 板块分层 / 剔除分解;三层不适用)→ 跑事件版 + 策略版 → 验收流程
照 #4 先例(审计 → 终签 → 人开卡)。
