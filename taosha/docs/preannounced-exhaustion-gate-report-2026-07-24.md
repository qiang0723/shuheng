# exp25 preannounced_exhaustion · 冻结前只读准确性窄闸报告(2026-07-24)

> 人令原文=`taosha/docs/preannounced-exhaustion-gate-order-2026-07-24.md`(F 条留痕 `403adae` 先行)。
> 全程只读(纯 SELECT);零生产代码、零库写、零 PAP、零冻结、零 manifest、零正式运行、零 persist;
> 未读取任何公告后收益、CAR、显著性或正式研究结果。
> 核验脚本+逐段输出=取证包 `~/shuheng/s25_gate_delivery_2026-07-24/`(SHA256SUMS 齐)。
> 本报告所有筛选参数(预增、上沿≥50%、前 20 日、相对行业超额≥10%、核验窗 2011-01-01≤锚<2024-07-01)
> **均为未冻结核验值**,不得据此冻结或运行;核验窗系借同族(exp8/exp12/exp13)研究期口径,非 exp25 裁定。

---

## 〇、结论(二选一)

**暂不可进入 PAP 草案。** 最小阻塞项(按硬度排序):

1. **行业收益基准数据前置缺口(硬阻塞,数据缺口)**:qbase/taosha 两库零行业指数、零行业收益实物
   (信息模式全表扫描 industry/index 命中 0);行业分类唯一实物=entity_master batch6 现值快照
   (2026-07-07 拉取,5,861 票,industry 零缺失,111 个行业值),**无历史版本、无变更表,不具 PIT 语义**;
   引擎 benchmark 能力=全市场等权+票池等权两种,无行业基准算法。登记条件"公告前 20 日相对行业指数
   超额≥10%"当前**不可计算**;遵令不发明代理口径,故**预跑组/未预跑组的分组量化本轮无法给出**
   (候选总数可量化,见§二)。待人裁:行业分类体系(选哪家、如何 PIT)+行业收益基准数据源与采集方案;
   或由人改条件口径(口径改动,工地不得代裁)。
2. **组间差判决形态待人裁定(阻塞 PAP 判决权威条款)**:现引擎判决机=单样本主窗 ADJ-BMP
   (`adj_bmp_main_only`),一切分组轴(exp20 direction/exp13 ST 轴)均为 NOT_FOR_VERDICT 诊断;
   代码面 grep 组间差/两样本判决能力=零命中。"预跑组 vs 未预跑组公告后反应差"作为顶层判决,
   要么需**新的组间差判决能力**(工程能力缺口,本单元零实现),要么由人改裁判决形态
   (例:主判决=预跑组单样本 ADJ-BMP,未预跑组固定 NFV 对照)。二者均属人裁。
3. **family 归属待架构窗口终核(登记令原文已注明,非新增)**:事实面=exp25 与 exp5(forecast_drift,done)、
   exp20(earnings_revision,done)共用同一数据实物 forecast batch1 与公告锚;但事件母体不同
   (exp25=首次公告条件事件;exp20=修正公告事件),且新增条件轴(事前相对强弱分组)。条件变体 vs 独立
   family 待人终核。

数据面本身基本干净(孤儿 0/锚零缺失/污染面全部可数、可 fail-closed 承接,见§二§三);
若上述 1、2 两项由人裁定落地,无其他数据性阻塞。

---

## 一、状态基线(令一,逐项实测)

| 断言 | 实测 | 判 |
|---|---|---|
| exp25=registered | `registered`(family=preannounced_exhaustion/trial 1,title=强预喜预跑组公告后反应差,source=human,verdict_power=full,registered_at=2026-07-12 12:25:07+08) | ✅ |
| frozen_at/result_json/done_at 空 | 三槽全 NULL | ✅ |
| 无 exp25 manifest/运行/遗留产物 | study_snapshot 全 10 行零 exp25/preannounced 命中;experiment_addendum exp25=0 行 | ✅ |
| 台账 25 行=14/2/8/1 | 25 行=registered 14/frozen 2/done 8/closed 1 | ✅ |

注:`pap_legacy_registry` 含 exp25 一行——核为 2026-07-13 修法#1 legacy-event 白名单**全表物化**
(25/25 全部实验各一行,status_at_migration=registered),属迁移期结构标记,**非遗留产物**,不构成基线不符。

## 二、数据与事件条件核验(令二)

数据实物=qbase `forecast_snap` **batch 1(唯一 tushare:forecast 批,2026-07-07,138,458 行**;
源拉 147,938 整行去重后 138,458,采集"同键 distinct 行照落"忠实存全)。

**① 预增/幅度上沿/首次公告日可否确定识别——可,带如下留痕:**
- `type` 十类零缺失,预增=39,578/138,458;`p_change_max`=幅度上沿字段,预增内非空 34,746;
- `ann_date` 零缺失、`end_date` 零缺失;`first_ann_date` 缺失 91/138,458;
- **首锚判定**:以 (ts_code,end_date) 为组(全域 117,482 组),组内 min(ann_date) 与 first_ann_date
  全等的 112,654 组(95.9%);**first_ann_date<min(ann_date) 即"真首次公告内容行不在库、仅存日期指针"
  = 4,738 组**(此类组无法从实物判定首次披露时点的 type/幅度,须 fail-closed);
  first_ann_date>min(ann_date) 矛盾=11 组;组内 first_ann_date 不一致=15 组。
- **公告锚=实际首次披露日可焊死**:ann_date 零缺失,锚取组内 min(ann_date) 并断言==first_ann_date,
  不等即 fail-closed;绝不触 end_date(报告期末)或修正日。——此为工程可行性陈述,实现口径待冻结。

**② 候选量化(核验值:首锚行=预增 ∧ p_change_max≥50;未冻结):**
- 全域候选 **27,762**/117,482 组;
- 核验窗(2011-01-01≤锚<2024-07-01,借同族口径未冻结)**21,945**;窗前(<2011)3,256;
  **holdout(≥2024-07-01)2,561,本轮零触碰**(仅计数,登记 holdout_start=2024-07-01);
- 逐年分布连续无空洞:2011:1,197/2012:800/2013:949/2014:1,200/2015:1,361/2016:1,744/2017:2,393/
  2018:2,210/2019:1,595/2020:1,560/2021:2,723/2022:2,049/2023:1,432/2024H1 含于窗内计数
  (2024 全年 1,312 中窗内部分已并入 21,945;取证包 S8/S9 有整年表与切分数);
- **预跑组/未预跑组:本轮不可量化**——分组条件依赖行业超额收益,行业基准无实物(§〇阻塞 1),
  遵令不建代理口径,直接列为数据前置缺口。

**③ ⚠实测事实(面向人,如实报,不代改):"上沿≥50%"在实物分布下几乎不构成筛选。**
预增内非空 p_change_max 34,746 行中 ≥50 的 34,525 行(**99.4%**);分位数 p1=50/p25=75.8/p50=116.8/
p75=229.4/p99=4,600。即登记条件实际≈"预增且幅度可判"。该参数是否达成筛选意图,待人冻结时裁定。

**④ 公告前 20 日数据充足性(严格 trade_date<锚,公告日零参与;daily batch 6):**
窗内候选 21,945 中锚前 ≥20 真实 bar 的 **21,113(96.2%)**;不足 20 的 832,其中**锚前零 bar=795,
经对照 entity_master.list_date 全部为公告日≤上市日(上市前/上市当日披露)**,余 37 为上市初部分不足。
个股价格侧无系统性缺口;前 20 日窗零未来数据的切割语义工程上可焊死(引擎估计窗机制既有)。

**⑤ 行业分类 PIT/行业收益基准(令二第 4-5 条):见§〇阻塞 1。**
分类实物=entity_master batch6 唯一批,现值快照无 PIT;行业收益基准=两库零实物、引擎零算法;
不发明代理,列为数据前置缺口。

**⑥ 缺失/重复/孤儿/方向不可判:**
- 幅度不可判:窗内首锚预增 23,571 组中 p_change_max 缺失 **1,410(6.0%)**(全域预增组 4,801);
- 孤儿:候选 ts_code 不在 entity_master batch6 = **0**;
- 重复:全键(ts_code,end_date,ann_date)多行组全域 12,826(采集忠实存全所致);窗内候选首日多行
  4,429,**但首日幅度值冲突=0 组、首日类型冲突全域仅 11 组**(预增∧非预增同日并存,fail-closed 面);
- 方向不可判:条件事件方向由 type 直接判定,type 零缺失,无方向不可判残余(幅度不可判已单列)。

## 三、污染面(令二末条)

| 污染类别 | 量(分子/分母/口径) | 承接方式(未冻结建议) |
|---|---|---|
| 同公司同日多公告(多报告期同日) | 窗内候选 6 公司日/12 事件;全域行级 252 (ts_code,ann_date) 多 end_date | 冻结时裁定折叠或 fail-closed |
| 修正公告混入 | 锚=min(ann_date) 结构上排除修正行(ann≠first 行全域 13,346 不入首锚);**真首行缺失 989 全域/414 窗内候选**=唯一混入通道 | fail-closed 剔除留痕 |
| 时序矛盾 | ann_date<first_ann_date 行 11;窗内候选组 first>min_ann 矛盾 4;first_incons 组 15;first NULL 窗内 2 | fail-closed |
| 区间字段缺失 | 窗内预增幅度缺失 1,410/23,571;p_change_min 单边缺失可回退面存在 | 两边界口径待冻结 |
| 区间字段冲突 | p_change_min>p_change_max = **0**;首日 pmax 值冲突组 **0** | 无 |
| 语义矛盾 | 预增而 p_change_max<0 = 45 行(全域) | fail-closed |

## 四、判决形态与复用边界(令三)

- **组间差可否由现有引擎确定性表达:不可**。判决机=单样本主窗 ADJ-BMP;分组轴一律 NFV(先例:exp20
  direction 轴、exp13 ST 轴、exp12 行业覆盖)。**需要新的组间差判决能力=是**(或人改判决形态,§〇阻塞 2)。
- **family**:待架构窗口终核(§〇阻塞 3;登记令原文预留)。
- **可复用**:forecast_snap batch1(唯一批,零孤儿);`explore_reader_forecast` 视图范式(max-batch 路由+
  holdout<2024-07-01 焊死+排北交所;**⚠现视图列面无 `type`**——exp20 结构性防误用设计,exp25 需自有
  视图暴露 type,属冻结后最小适配);bar_daily/adj_factor/trade_cal/entity 基础设施;study_snapshot/
  manifest 状态机;引擎估计窗/CAR/ADJ-BMP/NFV 诊断轴/report/driver/fixture 全套范式;
  earnings_revision_rules 的链键(ts_code,end_date,first_ann_date)/L2 确定性折叠/fail-closed 逐类计数范式。
- **真正缺失的最小件**(本单元零实现):①行业分类 PIT+行业收益基准数据(阻塞,人裁数据源);
  ②"公告前 20 日相对行业超额"条件计算件(依赖①);③组间差判决能力或判决形态人裁;
  ④exp25 专属 rules/driver/report 分支+含 type 读取视图(常规最小适配,同族先例,非阻塞)。

## 五、三分类清单(令四)

- **实测事实**:§一全表/§二①②④⑥全部数字/§三全表/行业零实物/引擎零组间差能力/视图无 type 列。
- **待人裁定**:行业分类与基准数据方案(或改条件口径)/组间差判决形态/family 归属/"≥50% 不筛选"
  是否维持/同日多期折叠口径/真首行缺失 fail-closed 确认/研究期与全部冻结参数。
- **工程能力缺口**:组间差判决件/行业超额条件计算件/exp25 专属最小适配三件套+读取视图。

## 六、边界遵守实录(令五)

零生产代码修改(仓库改动仅=令文留痕+本验收档+STATE);零数据库写入(两库全程 SELECT);
零 PAP/零冻结/零 manifest/零正式运行/零 persist;零参数扫描/零平台扩展/零顺手修复;
全程未读取公告后收益、CAR、显著性或任何正式研究结果(取证包脚本可复核:无一处涉后市数据)。
**完成窄闸,停交验点,等待下一令。**
