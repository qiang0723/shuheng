# 《切片 2(检验引擎)验收核对单》(定稿,随 v1.5 批复 2026-07-07 生效)

> 逐字落档(人下发 2026-07-07,开工前入档)。**原则:验收凭实物;每项要求可执行的证据(代码位置/输出样例/查询结果),不收结论句。**

## 一、算法正确性(对数段)

1. **estudy2 0.10.0 版本钉死**:安装自 GitHub 归档源,源码快照已入 shuheng 仓(路径留档)。
2. **对数范围**(改读 2026-07-07,附录 E · S2-DOC1 裁定):**BMP 段锚 estudy2(boehmer),逐点对数一致(±数值误差);ADJ-BMP(Kolari-Pynnönen 2010)段手算 + 零假设蒙特卡洛尺寸检验复核**(estudy2 0.10.0 无 KP 实现,附录 E1;不引替代参照)。spec 三法互证所涉检验;对数用例含至少一个多事件聚集场景(**与 E3 尺寸检验合并为同一 fixture**)。〔尺寸检验证据要求:朴素 BMP 拒绝率>α、ADJ-BMP 拒绝率≈α。〕
3. **对数口径对齐**:我方收益计算与 estudy2 multi_day=TRUE 同构(跨缺口收益),提供两侧同一样本的收益序列对照。
4. **分歧处理**:任何数值分歧逐笔归因,先核 estudy2 未决 issue(#12 CAR 计算疑点、#16 列名错误);分歧不自动判我方错;CAR 聚合处分歧附手算复核。

## 二、A股口径适配(estudy2 帮不上的部分)

5. **禁止零填充**:停牌/无交易日置 NA,代码中不存在任何以 0 填充收益缺口的路径(给出处理函数与单元测试)。
6. **估计窗最小覆盖门槛**:实现为冻结参数,值=**112/160(=70%)**(改读裁决 2026-07-07,见文末〔四口径拍板〕;原写"84/120"作废)。冻结估计窗=事件日前 250 至前 91 交易日(spec §5 行 66,共 **160** 交易日);窗内有效交易日 **≥112(=⌈70%×160⌉)** 方合格;不足门槛的事件剔除,剔除率进输出报告。
7. **事件落停牌期**:剔除逻辑实现;剔除率输出按年份分解;>5% 告警。
8. **涨跌停**:事件窗支持 [0,+2] 主窗 + [0,+5] 稳健窗;逐日 AR 分解为标准输出;板块(主板/创业板/科创板/ST)分层报告实现,含 2020-08-24 创业板 regime 边界处理。
9. **偏差方向声明**:输出报告模板含固定文本段,声明剔除类处置的保守偏差方向。

## 三、纪律接口

10. **口径参数冻结**:全部口径参数(门槛/窗口/剔除规则)从冻结配置读取,运行时不可覆写;参数变更走配置审计留痕。
11. **结果落库**:检验结果落库 append-only,与台账 Experiment 对象对接;N_eff 与剔除率同报(门②成色报告数据前提)。

---

> 施工依据:spec v0.2 + 附录 A/B/C/D + 本核对单。开工令要点:①引擎只认台账(拒 status≠frozen,从 pap_json 取事件定义);②reader 对合成 fixture 跑、接口签名按 explore_reader 契约写死(Q3 零改造)、不建 mock 视图、holdout 隔离验收归 Q3/切片3;③estudy2 按附录 D;④A股口径三条实现为冻结配置读取、运行时不可覆写;⑤验收=合成数据跑通、成本扣减与门槛校正人工可核、真实数据留切片3;⑥报告只陈述统计事实无建议口吻。

---

## 〔四口径拍板〕(2026-07-07,人裁 · 冻结配置 · 运行时不可覆写)

建 compute 前卡在的 4 个统计口径(①铁律)已人拍定死。四项均实现为**冻结配置**,运行时不可覆写(item 10 焊死)。

1. **compounding = continuous(对数收益)**。我方与 estudy2 两侧同设(对齐 item 3)。
2. **AR 模型 = SIM(单指数市场模型)**。regressor 按**冻结基准**取:池内假设 = 雷达股池等权;全市场假设 = 全市场等权。(否决"等权 market-adjusted";采市场模型 SIM。)
3. **覆盖门槛 = 冻结估计窗 160 日(前 250 至前 91)内有效交易日 ≥112(=70%)**,不足即剔、剔除率进报告(见 item 6 已改读)。
4. **ρ̄ 行业分组 = `entity_master` 的 tushare industry**(ADJ-BMP 的截面相关 ρ̄ 分组键)。

### 覆盖门槛改读裁决 + 错误归因(留痕,item 6 配套)

- **驳回**我方"84=最小估计期 / 120=稳健窗门槛"的语义方案——那是给数字倒推新语义。
- **实情**:"84/120" 本是"70%×120 日窗"的展开式(0.7×120=84)。**120 为架构窗口臆测、未核 spec §5** 的错误前提。
- **错误归因 = 窗口 II(在案)**:把 120 当估计窗是未核 spec 的臆测;spec §5(行 66)白纸黑字估计期 = 前 250 至前 91 = **160** 交易日。
- **密封实质 = 70% 覆盖率,维持不变;密封卡本体不改**。**112/160** 即 70% 在**正确分母 160** 上的实例化。
- **核对单 item 6** 之"84/120"**已改读"112/160(70%)"**(见上)。

### ρ̄ 已知近似注记(在案,不修)

- 行业分类取 `entity_master` 当前快照,**非 PIT**;对 ρ̄ 估计影响属**二阶**,在案不修。

### #2b 台账连带处置

- **`#2b`"上市满 120 日"(`listing_min`)参数不动**。
- 其"与估计窗对齐"理由**作废**(估计窗实为 160 非 120,对齐前提不成立);台账注记改读 **"人拍池定义"**。

> 留痕范围:密封卡本体不改;本澄清裁决 + 错误归因写入本验收文档(此节)与 STATE(`ops/STATE.md`);核对单 item 6 已改读;#2b 台账注记改"人拍池定义"。

---

## 〔item 7-11 开工两裁 + 施工设计〕(2026-07-07,人拍 + 我自决报留否决权)

开工令下发 item 7-11(三段串行:引擎骨架→A股口径件→纪律收尾)。开工前两处提请人裁、已拍定死;三条设计我自决、报留否决权。裁决留痕本 commit 先于施工代码(F 条)。

### S2-DEC2 · item 11 落库演示方式(人拍)

- **专设合成冒烟登记行**:新登记一条 `[SMOKE] slice2合成落库验收`,`source_type=llm`、`verdict_power=prescreen`、`contamination_note='切片2合成fixture,非真实结论,勿用于判决'`;走 `registered→frozen→running→done` **既有全路径**写 `result_json`(快照批次=`SYNTH`),**永久留台账**作 append-only 实证。
- **不写六条创始行**——保护其一次性 `result` 槽留给切片3真实数据(触发器一次性写入焊死、不可回滚);**不另建影子表/通路**——复用切片1 已终签台账触发器(item 11 原义"不另建通路")。
- 备选(事务内跑通即 ROLLBACK)已否决:不留 append-only 实证,与 item 11"落库"字面不符。

### S2-DEC3 · 事件窗 τ=0 锚点(人拍,消解 item 8 ↔ spec §5 潜在打架)

- **τ=0 := 首个可交易日 = T+1**(事件日 T 盘后披露→前视规避→观测自 T+1 起,spec §5"可交易时点=T+1、CAR 起点=T+1")。
- 主窗 **[0,+2] = T+1..T+3**、稳健窗 **[0,+5] = T+1..T+6**;逐日 AR 从 τ=0 记;**CAR 起点 = τ=0 = T+1**。
- 结论:核对单 item 8 之 `[0,+2]/[0,+5]` 与 spec §5 起点口径**对齐无冲突**,读法钉死(非 spec 让位)。

### 施工设计(我自决,报留否决权)

1. **reader 列契约**:spec 未给 `explore_reader` 列级定义(那是 Q3 真视图)。我方现定《explore_reader 列契约(淘沙侧要求)》供 Q3 **零改造照建**,列至少含 `(ts_code, trade_date, close, is_suspended, limit_flag/一字板, industry, board, is_st, first_ann_date, mkt_return)`;holdout `trade_date < 2024-07-01` 焊在视图 WHERE(reader 结构上拿不到)。
2. **fixture 分家**:`harness/make_fixture.py`(86e905f 对台证据,锚 estudy2)**不动**。A股口径(item 7/8)另建合成 fixture——**≥30 事件**(现 6 证券×1 事件=6 恒 INSUFFICIENT,跑不通完整路径)、**跨年**(剔除率按年份分解)、**跨越 2020-08-24**(创业板涨跌幅 regime 边界)、注入停牌/涨跌停/板块/ST。
3. **新增 A股参数**(各板涨跌停限幅、regime 日期、事件窗 [0,+2]/[0,+5]、样本闸 30)全部进 `frozen_config` 只读(item 10 焊死,运行时不可覆写)。
