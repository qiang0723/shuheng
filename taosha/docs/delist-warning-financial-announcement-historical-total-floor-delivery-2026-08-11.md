# exp22 v8 历史官方总数下界交付

时间口径：2026-08-11，Asia/Shanghai（UTC+8）。

## 一、授权与限缩解释

John 原文：

> 更新完就启动跑批吧

该回复紧接 v7 分页视图漂移与“更新后方可安全启动”的说明，登记为：允许完成不降级证据硬门的 v8 防误收修复；验收后推送、阿里云精确 fast-forward、从 51/646 断点启动全新 v8 监督链。不得解释为允许交集、并集、择一、多数、重试到碰巧一致，亦不授权数据库或研究状态写入。

## 二、修复

v8 同时具备两道新闸：

1. 同一遍分页的 `totalAnnouncement` 必须逐页完全一致；
2. `000607/2021-04-30` 新两遍接受集合的公告数不得低于 v6/v7 保全官方响应多次观测的下界 35。

第二道闸只消费历史官方响应的计数下界，不消费历史行集合；新数据仍须由 v8 两遍各自完整分页、ID 唯一、逐字段全等后形成。因此没有启用交并集、择一、容差或多数表决。

新写入只允许进入 `bisect_v8`；`bisect_v7` 加入旧布局白名单，仅作失败证据与合法 marker 的只读自验。

历史下界拆入独立小模块，未突破 300 行/60 行规模闸门，也未新增债务例外。

## 三、验收

阿里云钉版镜像 `shuheng-quant:579a354`（`sha256:e7b9b270…ebfc3`）以只读代码与临时 overlay 验收：

- bisection：`37/37 PASS`；
- index：`49/49 PASS`，含“稳定 32 仍因低于 35 下界被拒”“35 命中”“下界不外推其他证券”；
- localization：`6/6 PASS`；
- routes：`6/6 PASS`；
- `py_compile`：PASS；
- 规模：`242 files / 36,098 lines / 1,099 functions / debt 20+50`，PASS；
- 架构：`173 modules / 378 edges / cross-experiment debts=2`，PASS；
- `git diff --check`：PASS。

规模闸门首次拒绝索引器 307 行与 fixture 303 行；下界逻辑随后拆成 28 行独立模块，索引器回到 289 行、fixture 恰 300 行，未增加基线例外。

一次远端 overlay 首跑因工作目录仍在 `/opt/quant`，实际执行旧代码并显示旧计数 `34/34+46/46`；该结果明确作废。改以 `/tmp/overlay` 为工作目录后，模块解析命中新代码并得到上述 `37/37+49/49`，旧代码误跑日志保留、不冒充验收。

## 四、启动前硬基线

推送和阿里云 fast-forward 后必须重新执行同一套专项；随后只读核对：

- `routes=646`；
- 合法完成 marker=`51`；
- 首待办=`000607.SZ`；
- 历史错误=`7`；
- v7 raw reads=`359`；
- documents/contract/readback=`0/0/0`；
- v1—v7 容器、日志、错误记录和失败页均保留；
- v8 raw reads=`0`。

任一不符即停止，不创建 v8 容器。满足后才启动全新 `s22-ann-index-v8`；metadata 非零即 fail-closed，监督链不得自动重启。

