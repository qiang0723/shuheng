# exp10 `volume_drought_break` PAP 草案单元令（2026-07-29）

## 人裁原文

> 按B和默认项

## 被确认的待裁文本

人裁所称 B 与默认项，逐字对应上一轮交验请求中的下列文本：

- B：首次放量即终结；若当天不收阳则本阶段不构成事件，重新蓄积。
- 默认项：前60日均量不含当日、滚动均量、停牌断链、`close>open`、异常成交额 fail-closed、2011年起、一阶段至多一个事件然后重新蓄积。

## 本单元授权与边界

据此形成 NOT-FROZEN PAP 草案、按 B 口径只读重算候选漏斗、裁决映射与交付档。本单元不修改生产代码，不写数据库，不冻结，不生成 manifest，不读取事件日后收益，不运行正式研究，不 persist。草案交人复核后停交验点。
