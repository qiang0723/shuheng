# exp11 报告 tau_axis 个案展示修正 · 窄修回执(2026-07-24 七)

> 令链:窄修令留痕 `1f4c7ce`(07-24 六)→ 冲突上报 → 裁定令留痕 `1217d43`(07-24 七,选 C)
> → 施工 `aba4336`(与留痕分单)。
> 改判留痕:上一令「完整 exp11 报告『可交易』零命中」验收断言已由裁定令**显式作废**
> (误包含既有「可交易口径净收益」NFV 审计段,非同一语义);以裁定令三条断言面为准。

## 1. 施工(恰一处生产改动 + fixture)

- `engine/report.py` 逐日 AR 段头(单一渲染点):仅当 `audit` 含 `high_pullback_selection`
  (=exp11 报告路径)时,共用 `tau_axis` **展示**中「首个可交易日」→
  **「事件后首个有真实bar的价格观察日」**;runner 产值不动、统计内核零触碰、
  他实验渲染逐字节不变;注释内嵌"不建通用覆盖机制,同类需求另报人裁定,不得复制特判"。
- 「可交易口径净收益」段及「pap 既定可交易口径」注记:**原样,不撤段不改名**(裁定)。
- 一字板句:维持冻结 PAP 原文唯一权威「τ0一字板事件仅为价格观察,不得表述为可执行策略证据。」
  (渲染句与 fixture 断言均为原文,零改动)。

## 2. fixture 断言面(裁定令三条;adapter 26→29 断言)

1. exp11 渲染报告「首个可交易日」零命中(唯一来源即 tau_axis,整报告断言等价)+
   逐日 AR 段头含新 τ0 术语——PASS;
2. 新术语与冻结原文句在场——PASS;
3. 既有实验逐字节不变:exp8 回归探针=同 result 挂 exp8 键渲染,标题在位、
   **tau_axis 仍含「首个可交易日」=特判不外溢**——PASS;e2e 合成基线两台双跑
   `3116ba9b74f7c53b…4c22`==历史基线逐字节(默认路径无 exp11 键→替换代码不触发)。

## 3. 回归(两台)

- fixture:exp11 二件 29/29+24/24;各 exp adapter/rules/engine 件全绿(AWS 非 DB 套件全清单;
  aliyun 含状态机 46/46/pap 硬门 23/23/addendum 14/14);e2e 两台双跑==基线。
- 禁区遵守:零规则/漏斗/统计内核/数据视图改动(diff=report.py 单点+fixture);
  零 manifest、零正式收益读取、零正式运行、零 persist。

## 4. 待人

**停交验点:①验收本窄修回执与断言面 ②行为验收整体收口(承 07-24 五单元)→外部复核
通过后另令=exp11 自有研究 manifest→单次正式运行→persist;未令不动。**
