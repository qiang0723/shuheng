# exp22 公告索引第二次分页失败 · 日期二分窄修令

日期：2026-08-10（UTC+8 / Asia/Shanghai）

## 一、授权与唯一解释

上一交验点唯一未授权提案为：从自然年区间开始确定性递归二分日期，直到每个叶片首次请求
`hasMore=false`；每个单页叶片再独立读取两次，只有规范化行逐字相等才接受；单日仍多页、
双读不等、跨叶公告 ID 重复或日期越界均 fail-closed；新布局必须永久保留 flat v1 与
`annual_v2` 两轮失败实物。

John 随后回复原文：

> 授权

该回复只授权上述唯一提案的本地最小实现、攻击 fixture、治理闸门与交付留痕。它不授权删除或
覆盖任何阿里云证据，不授权推送、阿里云 fast-forward、重启容器或恢复监督链；这些动作仍须
另令。

## 二、不可变边界

1. 新布局另命名为 `bisect_v3`；flat v1 与 `annual_v2` 均只读保留。
2. 每次查询只用 `pageNum=1`。超一页即按日期跨度中点确定性二分，左右区间互斥且并集恰等
   父区间；不得跨页去重。
3. 单页叶片的 probe 与 confirm 必须是两件 write-once 原始响应；规范化并确定性排序后逐字段
   完全相同才接受。官方计数字段只保留观测；至少一个观测须与叶片行数相符。
4. 单日仍 `hasMore=true`、非终页不足 30 行、双读分页状态或规范化行不一致、页内/跨叶/跨年
   公告 ID 重复、证券混入、日期越界、请求件漂移、陈旧或缺失请求件均立即停止。
5. 既有 10 个合法 flat v1 done marker 继续自验；合法 `annual_v2` marker 保持兼容。失败目录
   绝不被解释为完成件。
6. 本轮零数据库访问或写入、零原件物化、零合同队列、零独立读回、零利润 PIT、零终版 PAP、
   零密封、零冻结、零 StudySnapshot、零 manifest、零收益、零研究运行、零 persist。

完成本地验证即停推送与远端续跑授权点。
