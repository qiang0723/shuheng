# exp22 公告索引 v5 · 双读漂移定位窄修令

日期：2026-08-10（UTC+8 / Asia/Shanghai）

## 一、v4 失败事实

`s22-ann-index-v4` 于 `2026-08-10 17:52:54+08` 在
`000046.SZ / 2021-01-01..2021-01-12` 触发“双读规范化行不一致”，metadata exit=`1`，
监督链记录 `metadata_v4_exit=1`，后三阶段零启动。停止实物为
`done=10 / bisect_v4_reads=494 / errors=4`。

两件请求逐字段相同、均为单页且 `hasMore=false`；pass A=`13` 行/总数 `13`，pass B=`12`
行/总数 `12`。共同 12 行逐字段完全相同，唯一差异为 pass A 多公告 ID `1209076074`；两件
写入时间相差约 1.44 秒。已完成的 172 组 pass A/B 中仅此一组不一致。证据只支持“上游结果
瞬时不一致”，不能越界断言具体为缓存、索引副本或负载均衡问题。

## 二、John 原文与限缩解释

在收到“最窄下一方案是多日双读不一致时继续确定性日期二分；只有各子区间双读完全一致才接受，
单日仍不一致继续 fail-closed”的诊断后，John 回复：

> 继续

本令将其限缩为本地 v5 规则实现、攻击 fixture、专项与治理闸门、交付留痕；不扩解为 GitHub
推送、阿里云 fast-forward、容器创建、监督恢复或重启。

## 三、唯一允许修复

1. 新写入布局固定为 `bisect_v5`；v1/v2/v3/v4 原始页、日志、错误和容器只读保留。
2. 多日区间的 pass A/B 若分页状态或规范化全集不一致，不接受任何一遍，保留两件原始响应后
   按日期中点确定性二分；父区间两次读取计入 raw read 守恒。
3. 子区间仍须独立 pass A/B 完全一致；不得把父区间任一集合下传、不得取交集/并集/多数、不得
   第三次重试至碰巧一致。
4. 单日单页 pass A/B 仍不一致即 fail-closed；单日多页继续执行 v4 两遍完整分页硬门。
5. 逐页身份、日期、唯一性、非终页 30、终页关闭、每遍计数闭合与最多 100 页规则不变。

## 四、停止线

本地验收完成即停推送与远端重启授权点。零数据库访问或写入、零利润 PIT、零终版 PAP、零密封、
零冻结、零 StudySnapshot、零 manifest、零收益读取、零研究运行、零 persist；E1 继续
`OPEN_FAIL_CLOSED`。

