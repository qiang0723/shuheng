# exp18 `audit_qualified` · 数据前置闭合报告

- 日期：2026-07-31（UTC+8 / Asia/Shanghai）
- 二元结论：**数据资产与污染量化已完成；首次披露语义硬门不通过，exp18 暂不可进入终版 PAP 或冻结。**

本轮严格执行人令中的失败出口：现有官方公告元数据通道不能为全部抽核样本证明
`fina_audit.ann_date` 等于审计意见原件首次披露日，因此停止，不更换事件锚，不生成源级快照，
不读取收益，也不进入研究运行。

## 1. 交付实物与版本

- 施工令：`taosha/docs/audit-qualified-dataclose-order-2026-07-31.md`
- 人裁留痕：`taosha/docs/audit-qualified-predata-rulings-2026-07-31.md`
- qbase DDL：`qbase/sql/025_audit_qualified_reader.sql`
- 专属采集件：`qbase/ingest/seed_fina_audit.py`
- 质量画像：`qbase/ingest/profile_fina_audit.py`
- 公告原文抽核：`qbase/ingest/verify_fina_audit_disclosure.py`
- 专项验证：`qbase/ingest/verify_fina_audit.py`，本地与阿里云均 `21/21 PASS`
- 代码链：`b0b8899`（数据路径）→ `3367237`（容器凭据注入）→ `64650cc`（质量验证）
  → `27aa7ac`（公告证据读取器收紧）→ `f4f4a94`（TRUNCATE 硬门）
- 内部证据包：阿里云 `/root/s18dataclose/`；递归 `SHA256SUMS` 共 93 项全部通过，
  13 类秘密扫描 `TOTAL_HITS=0`

本单元未把原始响应、公告元数据或 PDF 放入 Git；它们只住内部证据包。

## 2. 数据集、粒度与完整性

数据集粒度为 Tushare `fina_audit` 原始事实行；L1 不判定意见类别、首次披露、修订或事件资格。
同票同期源多行允许进入底表，消费方按已裁规则整组 fail-closed。

| 核验项 | 结果 |
|---|---:|
| 请求证券数 | 5,861（含退市票） |
| 返回证券数 | 5,612 |
| 请求公告日范围 | 2011-01-01 至 2024-06-30 |
| 原始响应行 / 整行去重后 | 64,157 / 64,157 |
| 精确重复 | 0 |
| `(ts_code,end_date)` 组 | 63,836 |
| 年度报告行 | 61,241 |
| `ann_date` / `end_date` 空值 | 0 / 0 |
| `audit_result` 空值 | 1 |
| `audit_agency` 空值 | 0 |
| `audit_sign` 空值 | 766 |
| `audit_fees` 空值 | 33,464（非事件必需字段） |
| 孤儿行 / 孤儿证券 | 0 / 0 |

正式事实批次为 `fact_batch=16`，source=`tushare:fina_audit`，落库时间
`2026-07-31 17:43:45.388865+08`。原始响应 JSONL SHA256 为
`e637366e99f396e6f8bc6dcaa1302f229d08e705653f1d7c65221ab80dc250be`；采集 manifest
SHA256 为 `62b2ce27e33952a2df5ce88fe92ef6599b879bda338acd61be8d352c3fe9ae69`。

## 3. 全量污染量化

研究窗年度报告共有 60,597 个同票同期组，互斥守恒为：

`60,597 = 必需字段缺失 1 + 多行 306 + 精确白名单目标 1,342 + 明确排除 58,948 + 未知 0`

意见分布：

| 审计意见 | 行数 | 组数 | 处置 |
|---|---:|---:|---|
| 标准无保留意见 | 58,273 | 57,968 | 排除 |
| 带强调事项段的无保留意见 | 1,287 | 1,286 | 排除 |
| 保留意见 | 793 | 793 | 候选 |
| 无法表示意见 | 543 | 543 | 候选 |
| 否定意见 | 6 | 6 | 候选 |
| 空值 | 1 | 1 | fail-closed |

污染面如下：

- 年度报告多行组 306，涉及 612 行；306 组均存在公告日冲突，意见冲突为 0；按人裁全部整组剔除；
- 精确事件白名单组 1,342；形成 1,334 个不同 `(ts_code,ann_date)` 键；其中 6 个键重复、
  涉及 14 行，按事件键重复整组剔除后，冻结前机械参考为 1,328 行；
- 上述 1,342、1,328 仅为数据质量参考，不是正式事件集、manifest 或运行硬断言；
- 三类意见只作组成 NFV，不形成收益分层判决轴。

## 4. 首次披露语义硬门

抽核采用官方巨潮公告元数据与原始 PDF：保留意见 8 个、无法表示意见 8 个、否定意见全部 6 个，
合计 22 个；各类别按公告时间均匀取样。通过条件同时要求：

1. 找到相应年度报告或审计报告原件；
2. 官方原件日期逐字等于 `fina_audit.ann_date`；
3. PDF 正文包含源表中的精确 `audit_result`。

首轮读取器把延期披露、独立董事意见、审计进展等标题误当成审计原件，仅 `5/22` 通过。
该轮结果保留为失败原件，不作为最终结论。随后只收紧原件识别并增加攻击 fixture，未改变样本、
锚或通过标准；第二轮结果为：

| 类别 | 通过 | 失败 | 失败形态 |
|---|---:|---:|---|
| 保留意见 | 7 | 1 | 官方元数据返回 0 条 |
| 无法表示意见 | 3 | 5 | 官方元数据返回 0 条 |
| 否定意见 | 2 | 4 | 官方元数据返回 0 条 |
| 合计 | 12 | 10 | 10 例均为官方元数据返回 0 条 |

第二轮找到原件的 12 例全部满足日期一致与 PDF 精确意见在场；但另外 10 例没有可用官方元数据，
不能据此证明首次披露语义。尤其否定意见仅 6 个候选而 4 个未取到官方元数据，硬门不能以
“已通过子集表现良好”替代。

最终抽核 JSON SHA256 为
`a31362266a237b6562d355c1ba7a8896dffb2b3f22748bacec0a15b8a8c575a6`，二元结论=`FAIL`。
这表示**当前证据不足以冻结 `ann_date` 为唯一事件锚**，不表示已证明该字段错误；两者不得混写。

## 5. append-only、视图与权限

- `fina_audit_snap` 对 UPDATE、DELETE、TRUNCATE 三类攻击均由触发器拒绝；攻击前后批 16 均为
  64,157 行；
- current 视图焊死 `ann_date<2024-07-01` 与排北交所，底表同口径计数与视图均为 63,789；
- `taosha_engine` 只获 current/snap 两视图 SELECT；current 读取成功，底表读取实测
  `permission denied`；
- snap 视图在未设置 StudySnapshot 时实测 fail-closed，不回退 current；
- 引擎会话本身 `transaction_read_only=off`，只读边界由数据库对象权限实现；本轮未扩展角色权限；
- qbase/taosha 的 StudySnapshot 均保持 18 行、最大 ID 363；未生成 exp18 源级快照或研究 manifest。

## 6. 状态与边界核验

- exp18 仍为 `registered`，`frozen_at/result_json/done_at` 为空；
- 台账仍为 26 行：`registered 8 / frozen 2 / done 14 / closed 2`；
- taosha 权威 StudySnapshot 无 exp18 备注；qbase 镜像与权威表均为 18 行、最大 ID 363；
- 未读取任何事件后收益、CAR 或显著性；零 PAP 终版、零冻结、零 manifest、零正式运行、零 persist；
- 阿里云代码 HEAD=`f4f4a94d7527e49015092eb0713ead74cdee2476`，工作树干净，无采集、画像或公告复核进程残留。

## 7. 失败留痕与证据边界

- 采集容器最初缺 `.env` 挂载及证据目录属主不匹配，均在数据库写入前停止；修正为环境变量注入与
  专用证据目录后才启动正式全量采集；
- 首轮公告抽核的标题误识别已如实保留；第二轮在新目录独立执行，未覆盖首轮原件；
- 数据库实物、官方元数据与 PDF、攻击测试和 SHA 清单位于阿里云内部证据包；Git 只保存实现、
  令文与本报告，不公开再分发原始响应或公告文件。

## 8. 停止线

本数据闭合单元到此停止。exp18 当前状态为：

**数据资产可用；首次披露语义硬门未闭；暂不可进入终版 PAP、方向密封、冻结或正式研究。**

若未来要恢复 exp18，须由 John 另令解决官方公告证据覆盖缺口；不得把本轮 12 个通过样本外推为
全量成立，不得改用 `end_date`、入库时间或后续更正日替代 `ann_date`，也不得在本单元继续寻找
代理锚或追加研究运行。
