# exp17 `earnings_flash_gap` 冻结前只读准确性窄闸令

日期：2026-07-30（UTC+8）  
授权人：John  
授权原文：**“继续下一个任务”**  
排产解释：沿既定顺序，在 exp16 正式闭卷后推进 exp17；本令只授权冻结前只读窄闸。

## 一、状态基线

只读确认：

- exp17=`earnings_flash_gap#1`、`status=registered`；
- `frozen_at/result_json/done_at` 均为空；
- 无 exp17 正式 manifest、运行记录或遗留产物；
- ledger 26 行，分布应为 `registered 9 / frozen 2 / done 13 / closed 2`；
- 本地与阿里云代码同 HEAD、工作区干净。

任一不符即停报人。

## 二、数据实物与来源可行性

1. 盘点 qbase/taosha 表、视图、批次、采集配置与证据目录，确认业绩快报
   `express` 是否已有实物；同时核对 forecast 现有批次、关键字段与覆盖。
2. 若 `express` 未入库，仅允许核官方文档、SDK 元数据与极小样本 API 探针；零批量采集、
   零落库、零缓存入仓。接口不可用或权限不足即如实列缺口，不发明代理。
3. 核验快报可用披露锚：实际首次披露日字段、后续修订/重复行、同票同期多行、
   `ann_date/f_ann_date/end_date` 时序；事件锚绝不用报告期末。
4. 核验快报实际净利润与 forecast 区间的字段语义、单位及可比性。单位、口径或合并层级
   不能确定时 fail-closed，不以同比幅度或文字类型替代实际值。
5. 核验 forecast 与快报的确定性连接键，至少覆盖 `(ts_code,end_date)`；量化或举证：
   多次预告/修正时“快报前最近一次公开预告”与“首次预告”两种基准是否会改变事件归属。
   本单元只呈事实，不替人冻结口径。

## 三、机械事件面与污染面

在数据允许的范围内，以未冻结机械读法核验：同票同报告期已存在快报前公开 forecast；
快报实际净利润严格高于预告上沿记 `up`、严格低于预告下沿记 `down`；落在区间内或恰等
边界不成事件。该读法仅供窄闸，不能据此冻结或运行。

报告须尽可能给出：

- forecast 与 express 各自行数、报告期组数、证券数、时间覆盖与缺失；
- 可确定配对数、up/down/inside/boundary、逐年分布；
- 预告多版本、快报修订、同日冲突、区间倒置、缺边界、缺实际值、单位不可判、孤儿快报；
- “最近公开预告”与“首次预告”的事件集合差异；
- 真实正反例及全部关键数字的分子、分母、阶段口径。

若仓内无全量 express，禁止用小样本外推全量候选数；相应项目明确记为不可量化。

## 四、判决形态与复用边界

只读核对：

- exp17 的 `up/down` signed 单判决是否可复用 exp20 `direction_signed_main`；
- 可直接复用的 forecast 视图、公告事件规则、cleaning、benchmark、报告与统计内核；
- 真正缺失的最小数据件、视图或事件适配件；
- 既裁“与 forecast_drift 不同 family”保持不重开。

本单元不实现任何缺失能力，不扩通用财务平台。

## 五、交付与停止线

结论只能二选一：

- `可进入 PAP 草案`；或
- `暂不可进入 PAP 草案`，列出最小阻塞项与恢复条件。

报告区分实测事实、待人裁定项和工程/数据缺口。全程禁止读取事件日后收益、CAR、
显著性或任何正式研究结果；零生产代码、零数据库写入、零 PAP、零冻结、零 manifest、
零正式运行、零 persist。仓库只允许本令、窄闸报告与 STATE 留痕。完成即停交验点。
