# Docker reader DSN 最小接线修复授权 · 人令留痕（2026-07-28，UTC+8）

> 上一停止档提出授权句：“授权最小DSN接线修复：reader按‘显式参数→环境变量→.env文件兜底’读取DSN，补fixture与零回归后停交Fable复核；不得改数据库权限、事件规则或统计逻辑。”
>
> 人的授权原文：**“继续”**。该回复明确指向上述最小修复；不得扩大。

## 边界

- 仅修改 reader 的 DSN 取值顺序并补攻击 fixture；
- 不输出或复制 DSN，不改变 `.env` 权限；
- 不改数据库权限、事件规则、PAP、统计逻辑或正式产物；
- 完成专项与零回归后停在 Fable 复核点；
- 本令不授权创建 exp24 manifest、正式运行或 persist。
