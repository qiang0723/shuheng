# exp22 公告索引 v4 · 单日多页窄修令

日期：2026-08-10（UTC+8 / Asia/Shanghai）

## 一、失败事实

`s22-ann-index-v3` 于 `2026-08-10 16:07:11+08` 在 `000046.SZ / 2017-04-25`
触发预设硬门“单日仍超过单页”，metadata exit=`1`；监督链记录 `metadata_v3_exit=1`，
documents、contract UNPROVEN queue 与 12 票 readback 均未启动。

停止实物：`valid_done=10 / bisect_v3_reads=320 / errors=3`。v3 页树 SHA256=
`29ef9951d09c0fbe1b62b9431e5aa3bf2cc4a60ce6d9a14151c4cdde976bba75`；v1/v2/v3
原始页、三轮错误日志与三个失败容器均须永久保留。

## 二、John 授权原文

> 授权单日多页 v4 窄修，验收后推送并从 10/646 续跑

## 三、唯一允许修复

1. 新布局固定为 `bisect_v4`；多日区间仍按 v3 规则确定性日期二分。
2. 单页叶片继续独立双读规范化行逐字相等。
3. 单日首次请求若仍 `hasMore=true`，仅该日允许分页；必须独立完成 pass A 与 pass B 两遍完整
   扫描。每遍从第 1 页开始，非终页恰 30 行、终页关闭 `hasMore`、最多 100 页，页内及跨页
   公告 ID 唯一，证券与公告日期必须命中该日；每遍实际行数须命中至少一个该遍 API 计数观测。
4. 两遍规范化全集按日期、时戳、公告 ID 确定性排序后须逐字段完全相等，方可接受该单日。
5. 任一遍重复、短页、超过 100 页、计数不闭、两遍全集不一致、混票、越界、请求件漂移均
   fail-closed；不得跨页去重、按交集或并集补齐、重试至碰巧一致。
6. v1/v2/v3 合法 marker 继续自验；旧失败证据不得删除、覆盖或迁移为 v4 成功件。

## 四、执行边界

本令授权本地实现、攻击 fixture、专项与治理闸门；验收通过后授权推送、阿里云精确 fast-forward、
远端复验并从合法 `10/646` 断点启动全新 v4 容器与监督链。只有 metadata 646/646 且当前运行
零错误，才可进入原件物化 → UNPROVEN 队列 → 12 票独立读回。

零数据库访问或写入、零利润 PIT、零终版 PAP、零密封、零冻结、零 StudySnapshot、零 manifest、
零收益读取、零研究运行、零 persist；E1 继续 `OPEN_FAIL_CLOSED`。
