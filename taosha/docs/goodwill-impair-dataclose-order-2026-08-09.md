# exp21 `goodwill_impair` · 数据闭合施工令

日期：2026-08-09（UTC+8 / Asia/Shanghai）

> John 在 exp14 正式闭卷、台账无后台研究任务后授权：
>
> 按照的建议继续

本句所指向的紧邻且唯一建议为：**单线推进 exp21 `goodwill_impair` 数据闭合，不并行恢复
exp18/exp23 或开启其他实验。** 本令将授权展开为下述自足边界；不扩解为终版 PAP、密封、冻结、
研究 manifest、收益读取、正式运行或 persist。

## 一、既有人裁原文与不可变口径

数据施工前须逐字保留下列 2026-08-07（UTC+8）人裁：

> 菜单A=A1；B=B1；C=C1；D=D1；E=E1；F=F1；G=G2，精确研究期起点待全量覆盖报告后由我另裁，
> 不得代填。st_policy=keep；postpone_policy=unified_announcement；其余沿承项按草案建议确认。
> 数据闭合另令，当前草案digest不得冻结。

施工与画像只按以下规则形成数据质量证据，不得据事件数改口径：

1. 分母只认事件前最近一份已公开、报告期与版本可证的
   `total_hldr_eqy_exc_min_int`；不得改用含少数股东权益或事件后年报权益；
2. 区间金额只有下限达到门槛才合格；上下限、币种或单位不可证即整组 fail-closed；
3. 分母空值、零、负分别计数并逐组剔除；不得取绝对值、补零或换字段；
4. 统一币种/单位后以 Decimal 精确 `>=0.05`，恰等入选，禁止 float；
5. 商誉与无形资产、其他资产或资产减值总额不可分离时整组 fail-closed，不拆分、不代理；
6. 业绩预告、专项公告、业绩快报和年报全部竞争最早合格披露；定性“可能减值”、金额待定、标题、
   报告期末及入库时点均不成事件锚；
7. 同一事项只取首次达到阈值可判条件的合格官方披露；后续修订只审计、不重复成事件；
8. 精确研究期起点尚未裁定，只能由全量连续覆盖、官方原件与失败分布形成菜单后交 John 另裁；
   不得按候选数量、方向比例、收益或显著性选择起点。

草案 digest
`61040e4f431034049e6dabd55cab28e9bebd395adf679ff65f24cea0916ef4f8`继续只作
NOT-FROZEN 历史锚，不得冻结或供 driver 使用。

## 二、施工前硬闸

只读确认，任一不符即停：

1. exp21=`goodwill_impair/trial1/llm/prescreen/registered`，`frozen_at/result_json/done_at`均空，
   addendum=0；
2. 台账26=`registered6/frozen2/done16/closed2`，无 running 行；
3. exp18、exp23仍为 registered 三槽空；exp1、exp6保持既有 frozen，不触碰；
4. StudySnapshot 三处现有身份不变，最大正式 ID=432；零 exp21 snapshot/manifest；
5. qbase 当前不存在可冒充本单元的新商誉减值额或财务报表 PIT 事实资产；现有批次、视图与生产表
   不得覆盖、原地改写或补历史值。

## 三、授权施工范围

### 3.1 财务报表 PIT 资产

1. 只建设本命题所需的 append-only 资产负债表 PIT 批次与最小 current/snap 只读视图；至少忠实保留
   `ts_code/ann_date/f_ann_date/end_date/report_type/update_flag/goodwill/
   total_hldr_eqy_exc_min_int/total_hldr_eqy_inc_min_int`及批次、valid/observed time；
2. L1 只忠实存真，不判事件资格、不折叠版本；UPDATE/DELETE/TRUNCATE 三路结构性拒绝；底表不给
   研究角色直读，只授权最小视图 SELECT；
3. 全量采集须有显式字段双向漂移断言、分页/截断断言、响应混票拒绝、断点续传一致性、请求全集终态
   恰等断言、批次重复双重拒绝；单事务落批与行数恒等，失败整体回滚；
4. 不建设利润表或财务指标代理来填商誉减值分子；如确需新增超出上述字段/接口的财务资产，先停报人。

### 3.2 官方公告原件与商誉减值金额证据

1. 建立 append-only 官方公告索引与证据件，覆盖人裁 F1 的四类载体；每件保留官方 URL/公告日、
   证券、报告期、载体、原件 SHA、抓取时点、解析器版本及原文证据定位；
2. 金额证据只能来自官方原件正文，结构化记录精确金额/区间上下限/币种/单位、商誉专属性、事项身份、
   修订关系与可判状态；原件永久保留，解析结果不得覆盖；
3. 允许使用标题或现有 forecast 文本**仅作召回候选定位**，不得进入事件资格、金额、首次披露或
   覆盖率判定；必须量化候选召回的误检与可见漏检边界；
4. 禁止使用 `goodwill`余额、`assets_impair_loss/impai_ttm/q_impair`资产减值总额、后验年报金额、
   标题关键词、报告期末或 observed_time 冒充分子或公告锚；
5. 同票同报告期/同事项多件、日期冲突、金额冲突、载体冲突或修订关系不可判，按既有人裁整组
   fail-closed，不任取最早/最晚/最大金额。

### 3.3 全量量化与抽核

交付必须同时给出分子、分母、逐年与逐载体明细，并以互斥恒等式闭合：

1. 财务 PIT 原始行/组、版本、逐年覆盖、两种权益与 goodwill 缺失、报告期/公告时点异常；
2. 公告候选、官方原件可定位/不可定位、精确金额/区间/定性未量化/组合不可分离、币种单位异常、
   同事项修订、多行与冲突；
3. 首次合格披露可证/不可证、事件前归母权益可证/缺失/零/负/版本不可判、Decimal 阈值分类；
4. 按年度与载体分层抽核原件，逐例给通过/失败原因；结论只能是“可证明”或“不能证明”，不得写
   “基本可证”；
5. 仅输出由证据可支持的连续研究期起点菜单及代价，**不代填起点**；候选数量仅为 NFV 数据质量
   参考，不得成为选起点依据。

## 四、源级 StudySnapshot 与失败出口

1. 只有财务 PIT、公告原件/金额证据、全量量化与抽核四部分全部通过，才允许生成一次 exp21 数据
   闭合用**源级** StudySnapshot；须记录完整批次向量、三处 content/digest 一致与反冒充声明；
2. 该源级快照不是 exp21 研究 manifest，不授权收益读取或正式运行；
3. 若首次合格披露、商誉专属金额、同事项修订链或连续历史覆盖任一不能可靠证明，立即停止在数据
   硬门；不得换用关键词、资产减值总额、商誉余额、后验财报、标题或其他代理，不得自动缩短研究期；
4. 如公告原件工程需要未授权的通用平台扩建、OCR/人工判读规模超出本单元最小闭合边界，先列最小
   阻塞项停报人，不得顺手建设。

## 五、代码、数据库与安全边界

1. 只允许 qbase 专属采集、L1 DDL、最小 current/snap 视图、公告证据、画像/抽核脚本、专项 fixture
   及本单元文档；`taosha/engine/runner.py`、`cleaning.py`、研究 driver/report/PAP schema 与既有
   闭卷产物零触碰；
2. 新 Python 文件≤300行、函数≤60行；存量规模债务例外不得增长，`python -m ops.verify_code_size`
   必须通过；优先复用既有批次、快照、append-only 与公告抓取原语，不复制平台逻辑；
3. 凭据只通过环境注入且不回显；原始响应与官方原件不得提交 Git；交付取证执行 SHA256 清单及
   13类秘密扫描；
4. 全程零收益读取、零 CAR/显著性、零终版 PAP、零密封、零冻结、零研究 manifest、零正式运行、
   零 persist；exp18/exp23不并行恢复。

## 六、交付与停止线

交付：人裁逐字留痕、提交链与触碰面、批次/视图/触发器身份、全量守恒、逐年覆盖与失败分布、
分层抽核、研究期起点菜单、源级快照身份（仅在硬门全过时）、专项 fixture/数据库攻击验证、规模闸门、
证据目录 SHA 与秘密扫描。完成后停止在数据闭合交验点，等待独立复核与 John 对精确研究期起点另裁。
