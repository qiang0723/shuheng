# exp19 `dividend_surprise` · 冻结前只读准确性窄闸令

- 排产裁定：2026-08-02（UTC+8 / Asia/Shanghai）
- 原排期：2026-08-04
- 实际执行授权：2026-08-05（UTC+8 / Asia/Shanghai）

## 一、John 原文

> 转向公司公告类，08-04 排 exp19 dividend_surprise 冻结前只读准确性窄闸。

> 授权，执行

第二句在当前上下文中的唯一指向，是执行已经排定但尚未启动的 exp19 冻结前只读准确性窄闸。

## 二、状态基线

只读确认：

- exp19=`dividend_surprise/trial 1/llm/prescreen/registered`；
- `frozen_at/result_json/done_at`均为空；
- 无 exp19 StudySnapshot、正式 manifest、正式运行或遗留产物；
- 台账总量与状态分布同当前数据库实物。

任一不符即停止并上报。

## 三、数据与事件准确性核验

登记命题保持原文：年度每股分红较上年变动≥50%（阈值待冻结，增/减分层）的预案公告日为事件；方向为增正、减负。50%只作为本轮机械核验值，不据此冻结或运行。

只读核验：

1. qbase、taosha 与当前依法可读数据源中，是否已有年度分红、每股分红、预案公告日、实施进度、修订状态、基准股本等实物；
2. “年度每股分红”能否由结构化字段确定性表达，税前/税后、现金/送转、年度报告期与跨年比较不得混写；
3. 公告锚能否确定为实际预案公告日，不得用报告期末、实施公告日、股权登记日、除权日或入库时间替代；
4. 预案、股东大会通过、实施、未通过等阶段行，以及 `update_flag` 修订行、同票同期多值、多公告日、字段缺失与重复键的污染面；
5. 上年分红为零、上年记录缺失、当年为零时，“同比变动≥50%”是否数学可判；不可判时不得善意补零或发明无穷增幅；
6. 在不读取任何事件后收益的前提下，以不超过30只证券的确定性形态探针量化字段、阶段、版本和候选漏斗；探针只验结构，不外推全市场频率；
7. 增/减方向能否复用既有 `direction_signed_main` 单事件集、单顶层 `adj_bmp_main_only` 判决路径，以及真正缺失的最小件。

现有数据不足时，只列最小数据前置与待人裁定项；不得补抓全量、落库、发明代理口径或建设新能力。

## 四、交付与停止线

结论只能二选一：

- `可进入PAP草案`；或
- `暂不可进入PAP草案`，列出最小阻塞项。

报告须区分数据库实测、官方文档、限量探针、待人裁定与能力缺口，关键数字给出分子、分母和口径。

本单元零生产代码修改、零数据库写入、零全量采集、零PAP、零冻结、零manifest、零收益读取、零正式运行、零persist；只允许本令、窄闸报告与 STATE 留痕进入仓库。完成即停交验点。
