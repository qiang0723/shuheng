# exp10 `volume_drought_break` PAP 草案单元交付（2026-07-29）

## 1. 结论与实物

- 草案：`taosha/docs/volume-drought-break-pap-draft-2026-07-29.json`；状态明确为 **NOT-FROZEN**。
- 顶层18键，沿既有事件版PAP结构；`validate_pap=PASS`，检验窗解析为`(5,20,60)`。
- 文件SHA256与引擎canonical digest均为`d2c67942713b75bec76482c51223d77ecf2c142a8e0fa9020932f2f5dc409170`；文件本体为`sort_keys=True`、紧凑分隔符、UTF-8、末尾单换行。
- 本digest仅标识草案，不得用于冻结、driver或正式运行。

## 2. 人裁落键

人裁原文为“按B和默认项”，对应令文中的B及默认项，不作善意扩写：

| 人裁项 | PAP键位 | 落地语义 |
|---|---|---|
| B：首次放量即终结；不收阳则整段不成事件 | `event_def` | armed后首个`amount>prior60_mean`为唯一终局；`close>open`才入事件，否则拒绝并重新蓄积，不继续等待 |
| 前60日均量不含当日、滚动均量 | `event_def` | 只取当前bar之前60根有效真实bar；阶段内逐bar滚动 |
| 停牌断链 | `event_def`、`cleaning` | 交易所日历缺bar打断低量段及armed状态，不暂停 |
| `close>open` | `event_def` | 严格收阳；相等或收阴均按非收阳终局拒绝 |
| 异常成交额fail-closed | `event_def`、`cleaning` | NULL/非正amount或缺open/close打断状态且不入均量 |
| 2011年起 | `event_def`、`bias_statement` | `2011-01-01≤event_date<2024-07-01` |
| 一阶段至多一个事件后重新蓄积 | `event_def` | 事件或非收阳拒绝均终结阶段；后续须重新连续形成≥5日低量段 |

机械边界同步写死：低量严格`<30%`；放量严格`>100%`；位于`[30%,100%]`的bar不终结armed阶段。两类恰等边界本批次均为0，但实现不得依赖样本为空。

## 3. B口径只读重算

输入仍为`daily batch6`、`trade_cal batch10`；读取字段仅`ts_code/trade_date/open/close/amount`，其中open/close仅用于事件日收阳谓词，未读取事件日后收益、CAR或显著性。

双跑逐字节一致：

- result SHA256：`989a4ba2090ef1c5dab40f9445e8ef34eacad2ea120a0fa1ff4ab42afde257ec`；
- selection SHA256：`3dc4e83be46a3354cdd056995d4ec1a33a35b5ec5f0a97788d31f4847d08e0b9`；
- 脚本SHA256：`98b2a41bb075f028a70e1eef6ddc60adac67ba424d69fae1d678b864cf7c4cde`。

漏斗守恒：

```text
armed 38,417
= 收阳首次放量事件 27,467
+ 非收阳首次放量拒绝 7,204
+ 日历缺bar打断 3,328
+ 异常bar打断 1
+ 右删失 417

首次放量终局 34,671 = 事件 27,467 + 拒绝 7,204
事件 27,467 = pre2011 13,578 + 研究期 13,889
拒绝 7,204 = pre2011 3,709 + 研究期 3,495
```

研究期事件`13,889`、涉及`4,137`票；逐年为：2011 1303 / 2012 1129 / 2013 575 / 2014 669 / 2015 466 / 2016 640 / 2017 1001 / 2018 942 / 2019 1610 / 2020 1463 / 2021 1221 / 2022 1254 / 2023 1190 / 2024H1 426。最重年2019=`1,610/13,889=11.59%`，最挤日2020-11-09为78事件。

旧A口径`17,220`自本裁决起作废，不进入PAP。B口径会在非收阳首次放量后重新蓄积，因此新旧事件集不是简单包含关系；不得把`−3,331`解释为逐条剔除数。

## 4. 草案结构与报告边界

- 唯一判决候选仍为主窗ADJ-BMP，辅助方法和所有漏斗/聚集/非收阳拒绝统计均为`NOT_FOR_VERDICT`。
- 非收阳拒绝组只报事件几何计数，不读取或展示其后续收益，不形成对照组或第二判决。
- `13,889`仅为daily6/cal10下的NOT-FROZEN对账参考，不是正式运行硬断言；正式数量以未来exp10自有manifest与冻结规则确定性产出为准。
- 偏差声明只限定冻结规则存活样本，方向未知；不得表述为可执行策略证据。

## 5. 终版前一次确认清单

以下沿承项不在本次“B和默认项”的逐字范围内，草案先按既有事件版写入建议值，终版前须人一次确认，不视为已裁：

1. τ0=`event_date`之后首个有真实bar的价格观察日，`missing_bar_only`顺延≤5；三窗`5/20/60`；
2. 估计期`250~91`、覆盖门`112/160`、`sample_gate=30`；
3. 全市场等权benchmark与`adj_bmp_main_only`；
4. cost四值仅schema/执行审计，不控制CAR取样；
5. holdout、field roles、digest binding及无收益分层轴的结构沿承。

若人确认，下一单元只做终版文本收口与新digest；预判仍须在终版复核后由人亲拟并绑定终版digest。

## 6. 边界

本单元零生产代码、零数据库写入、零冻结、零manifest、零事件日后收益读取、零正式运行、零persist。重算证据留于阿里云`/root/s10pap/`；完成即停交验点。
