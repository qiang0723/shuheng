# exp8 冻结前回修单元 · 人令留痕(2026-07-17 深夜二,F 条,原文即口径)

## 人令原文(逐字)

批准启动 exp8 冻结前回修单元。按本轮复核裁决修订 C1–C6,并逐项修复四个 P1。授权触碰:PAP/生成器设计、limit_open_rules.py、构造 fixture,以及最小范围的 cleaning.py、runner.py、report.py;所有新增策略必须显式参数化,既有默认行为及既有实验输出零回归。
必验:T+1停牌顺延1/5/6日边界;st_policy=reject/keep;辅助方法反向不得改变 adj_bmp_main_only 判决;上市日前历史bar、缺失list_date及上市区间异常fail-closed;所有非权威结果结构化 NOT_FOR_VERDICT;exp8偏差声明改为"方向未知、估计对象限于存活样本"。
回修后交付最终PAP JSON及digest、触碰面、fixture结果、既有全家福回归和默认路径逐字节零回归证明。仍禁止写driver、读取收益、生成manifest、写台账或运行正式研究;本次不构成冻结令。

## 工地接令注记(执行侧解读,如与人意不符以人裁为准)

**C1–C6 裁决映射(依必验清单锚定):**
- **C1(τ=0 锚定)= 按引擎实物**:τ=0=事件日后首个可交易日,一字/停牌顺延,MAX_POSTPONE=5 超限剔 postpone。必验锚=顺延 1/5/6 日边界(1=顺延收、5=顺延收上限、6=剔除)。
- **C2(ST 处置)= 乙案**:`st_policy` 显式参数化——`'reject'`(默认,spec §5 原行为,既有全部路径零回归)/`'keep'`(exp8 冻结值:ST 事件入主样本与主判决,ST/非ST 分层报告)。
- **C3(recent_listing 层无 CAR)= 甲案(如实预注册)+ P1 偏差声明改写**:exp8 偏差声明統一为"**方向未知、估计对象限于存活样本**"(作废承 exp4 的"保守方向"措辞——本假设剔除与效应方向关系不可知,不得宣称保守)。
- **C4(重复映射)= 维持现实现**(涉事链全部剔除、逐条上报):人令未另裁,按已交实现执行;如人意为合并案,请驳回本条注记。
- **C5(交易龄)= 维持自身行序号≤30,新增 listing 锚定 fail-closed 验证**:上市日前历史 bar / 缺失 list_date / 上市区间异常(delist≤list、bar 越出上市区间)→ 该票事件 fail-closed 剔除留痕,不猜不补。
- **C6(非权威结果)= 结构化 NOT_FOR_VERDICT**:所有非权威块(次级窗/稳健窗/三辅助法/各分层)带结构化 `not_for_verdict` 标记+渲染标注;经显式参数启用(exp8 冻结值 on,默认 off=既有输出逐字节不变)。

**四个 P1 映射(=必验清单第 3–6 项):**
- P1-1 判决隔离:verdict 唯一消费主窗 ADJ-BMP(`adj_bmp_main_only`),辅助方法(朴素 t/Corrado 秩/日历时间法)反向或反显著不得改判——以可区分 fixture 直接验证。
- P1-2 上市区间 fail-closed(见 C5)。
- P1-3 非权威结果结构化 NOT_FOR_VERDICT(见 C6)。
- P1-4 偏差声明改写(见 C3)。

**边界重申**:本次不构成冻结令;仍禁写 driver、读收益、生成 manifest、写台账、运行正式研究。
交付=最终 PAP JSON+digest、触碰面、fixture 结果、既有全家福回归、默认路径逐字节零回归证明。
