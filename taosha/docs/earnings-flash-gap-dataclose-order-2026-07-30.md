# exp17 `earnings_flash_gap` 数据前置闭合施工令

日期：2026-07-30（UTC+8）

授权人：John

授权原文：**“继续下一个任务”**

## 一、目标

只闭合 exp17 冻结所需的两项数据前置：

1. `express` 全量事实批次、append-only 承载表、holdout 读面和源级快照；
2. 菜单B的全量异常形态量化，以及菜单C的公司公告会计口径抽核。

本单元不是PAP终版、冻结或研究运行单元。

## 二、采集范围与事实口径

- 来源固定为 Tushare `express_vip`，显式请求 `update_flag`；不得改用代理源或文字类型回填；
- 范围只覆盖研究用途所需报告期：`2010-12-31`至`2024-03-31`的季度末，按季度分片全量获取；
  不为A股估计窗向前扩，不扩成通用财务采集平台；
- L1忠实存储源返回，包含退市证券；只做整行去重，不按业务键择一、合并或修正；
- `valid_time=ann_date`，缺失时才使用批次as-of；`observed_time`为本批实际采集时刻；
- 原始分片响应、字段清单、行数与SHA进入内部证据包，不公开再分发，不写入Git。

## 三、工程最小件

1. qbase新增一份幂等DDL：`express_snap` append-only表、必要索引、现值视图与StudySnapshot路由视图；
   holdout `ann_date < 2024-07-01` 在两只消费视图中焊死；只授予 `taosha_engine` 视图SELECT，底表不授权；
2. 新增专属 `seed_express.py`：小函数、单一职责、显式字段、季度分片、截断/字段漂移/空批次
   fail-closed、整行去重、单事务落一个 `fact_batch(source='tushare:express')`；
3. apply DDL后先dry-run，再正式采集一次；成功后生成并发布新的源级StudySnapshot，镜像与attestation
   digest逐字一致；不得生成exp17研究manifest。

## 四、质量核验与菜单量化

只读产出以下分子、分母、比例、逐年分布和恒等式：

- 总行数、证券数、报告期组数、公告日期覆盖、空值、精确重复与业务键重复；
- `update_flag=0/1/缺失/白名单外`的行数与组数；
- 同票同期flag0缺失、多条、`ann_date/n_income`完全一致重复及字段冲突组数；
- 按已裁A1（快报前最近一次公开完整预告）配对后的 `up/down/inside/boundary`、孤儿、区间不完整、
  同日预告排除及逐年分布；不得重算A2用于选口径；
- 若flag1为0，明确“对express修订的实测判别力未建立”并量化菜单B两案的样本损失；
- 抽取若干正利润、亏损及边界附近真实公司快报公告，逐条核`n_income`是否等同于预告净利润区间
  所指归属范围；给原文来源、报告期与结论，不得以字段名猜测。

## 五、停止线

- 任一季度接口失败、字段缺失、疑似截断、落库核行数不等、快照三处不一致，立即停止并报人；
- 零收益读取、零事件后行情、零PAP终版、零冻结、零研究manifest、零正式运行、零persist；
- 不修改统计内核、runner、cleaning、既有result或闭卷产物；不顺手建设通用财务平台；
- 完成交数据质量报告、DDL/采集件、批次与快照锚、内部证据包SHA清单，停交菜单B/C裁定点。
