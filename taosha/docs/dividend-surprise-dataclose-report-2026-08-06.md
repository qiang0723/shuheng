# exp19 `dividend_surprise` 数据闭合技术报告

- 日期：2026-08-06（UTC+8 / Asia/Shanghai）
- 实验：exp19 `dividend_surprise`
- 状态结论：**数据资产闭合；B2 研究期仍待 John 裁定；不可进入终版 PAP、冻结或运行**
- 施工令：`taosha/docs/dividend-surprise-dataclose-order-2026-08-06.md`
- 草案锚：`b61c1a00e58181d6756b7ea8d06b15638104d5f2c267c3986ecc1252dcf47c9b`（NOT-FROZEN，仍不得冻结）

## 一、答案先行

1. Tushare `dividend` 全量事实批、current/snap 最小只读视图和源级 StudySnapshot 已闭合；append-only 与引擎底表拒读均实测生效。
2. 历史初始预案覆盖存在结构性断点：财年 2011–2015 为 0，2016–2017 各仅 1 组，2018 起严格 E1 可识别率跃至 99.7% 以上。
3. 公告原件抽核 v2 为 **19/20**：公告年 2020–2024 为 **15/15**，唯一失败位于 2019 年，`603259.SH` 表内 `cash_div_tax=0.58002/股`，同日官方原件为 `0.58/股`，不得按舍入视作相等。
4. 因年度变化事件必须消费上一年度初始预案值，`2020-01-01` 起点仍会碰到 2019 年失败证据；最早可进入人裁菜单的连续起点是 **`2021-01-01`**。
5. 本报告不代裁。B2 只保留：`B2-P1=2021-01-01`，或 `B2-STOP=停止 exp19`。更晚起点没有新增证据优势，只会增加自由度并缩短样本，故不另列。

## 二、数据资产与血缘

| 项 | 实物 |
|---|---|
| 正式事实批 | `fact_batch=17`, source=`tushare:dividend` |
| 拉取时点 | `2026-08-06 11:06:00.299851+08` |
| 全量请求 | 5,861 证券逐票请求 |
| 原始/去重后行数 | `177,873 / 177,873` |
| 源内证券 | 5,824 |
| 源公告日范围 | `1991-03-17..2026-08-01` |
| current 视图 | 126,900 行，焊死 `ann_date<2024-07-01`、排北交所 |
| 源级 StudySnapshot | `375` |
| 三处 digest | `2df8e289a7c1dbacebf571db100d36d4786e4af2b994018a795882a7d4c7a6b7` |
| qbase 新键 | `dividend=17` |
| 研究身份 | 仅源级快照；**不是 exp19 研究 manifest** |

快照 375 的 taosha 权威行、qbase mirror 与 publication attestation 三处 digest 逐字相等；engine 经 snapshot 375 读取 snap 视图为 126,900 行，与 current 同批同规则计数相等。

### Append-only 与权限攻击

| 攻击 | 结果 |
|---|---|
| `UPDATE dividend_snap` | 被 `_freeze_appendonly` 拒绝 |
| `TRUNCATE dividend_snap` | 被 `_no_truncate` 拒绝 |
| `taosha_engine` 直读底表 | permission denied |
| 攻击后 batch17 行数 | 177,873，零变化 |

## 三、全量画像与守恒

### 3.1 年度范围

- 年度范围行：97,543。
- 年度组：52,027。
- 阶段行守恒：`预案 26,575 + 股东大会通过 26,906 + 实施 43,963 + 未通过 83 + 停止实施 13 + 其他 3 = 97,543`。
- 版本行守恒：`update_flag=0 95,999 + update_flag=1 1,544 = 97,543`。
- E1 分类守恒：`qualified 26,467 + initial_missing 25,560 = 52,027`；多条初始行、字段冲突、负现金初始行均为 0。
- 后续值不得回填：25,558 组虽有非预案阶段现金值，但无严格初始预案。

### 3.2 污染量化

独立 SQL 对账结果：

| 质量面 | 数量 | 分母/说明 |
|---|---:|---|
| 多阶段组 | 26,487 | 52,027 年度组；可与下列项重叠 |
| 多公告日组 | 78 | 组内非空公告日多于 1 |
| 多 `cash_div_tax` 值组 | 936 | 阶段/修订值发生漂移 |
| 含 `update_flag=1` 组 | 955 | 修订链存在 |
| 无严格初始预案组 | 25,560 | E1 fail-closed |
| 有后续值但无初始预案组 | 25,558 | 严禁回填 |
| 无初始且无后续现金值组 | 2 | 不可判 |
| `cash_div_tax` 空行 | 3 | 97,543 年度范围行 |
| `base_date` / `base_share` 空行 | 各 15,986 | 仅披露，不用于回填 A1 |
| `update_flag` 空行 | 0 | 97,543 年度范围行 |
| 完全重复行 | 0 | 177,873 全量行，L1 整行去重后 |

组级指标允许重叠，不应相加为“污染总组数”。

### 3.3 A1/C1/D1/E1 机械漏斗

26,467 个严格初始预案组恰分为：

| 结果 | 数量 | 读法 |
|---|---:|---|
| up | 2,679 | 相邻财年 `change>=+50%` |
| down | 3,436 | 相邻财年 `change<=-50%` |
| inside | 8,596 | 可判但未到阈值 |
| zero_undefined | 6,501 | 上年明确为 0，百分比无定义 |
| missing_prior_group | 2,451 | 无相邻上年组 |
| unresolvable_prior | 2,804 | 上年组存在但初始值不可判 |
| **合计** | **26,467** | 恒等成立 |

恰等 `+50%/-50%` 共 698，已包含在 up/down 中，只是边界诊断，不能重复加总。

## 四、年度覆盖断点

| 财年 | 年度组 | E1 可识别组 | 覆盖率 |
|---:|---:|---:|---:|
| 2011 | 1,643 | 0 | 0.000% |
| 2012 | 1,809 | 0 | 0.000% |
| 2013 | 1,896 | 0 | 0.000% |
| 2014 | 1,988 | 0 | 0.000% |
| 2015 | 2,025 | 0 | 0.000% |
| 2016 | 2,427 | 1 | 0.041% |
| 2017 | 2,791 | 1 | 0.036% |
| 2018 | 3,600 | 3,588 | 99.667% |
| 2019 | 3,829 | 3,827 | 99.948% |
| 2020 | 4,290 | 4,286 | 99.907% |
| 2021 | 4,705 | 4,697 | 99.830% |
| 2022 | 4,970 | 4,967 | 99.940% |
| 2023 | 5,106 | 5,100 | 99.882% |

这不是平滑缺失，而是 2018 财年前后的结构性源覆盖切换。30 票窄闸的“2019 断点”不能直接代填研究起点，但全量画像确认了相同风险形态。

## 五、公告原件证据

### 5.1 首轮与窄修边界

首轮自动抽核固定为：同一 `ann_date`、独立预案标题、原件含“每 10 股税前现金值”的精确匹配，结果 `15/20`。

首轮保留不覆盖。其四个“同日无候选文档”实际在巨潮索引中存在同日官方原件，原因是标题/单位识别不全。v2 只修正原件识别：

- 纳入年度报告/摘要、`分配利润预案`词序变体和特别分红方案；
- 接受官方原件以“每股”或“每 10 股”披露的等价单位；
- `ann_date` 必须精确相等；
- `cash_div_tax` 换算后必须 Decimal 精确相等；
- 不引入金额容差，不更换公告锚，不读取实施值。

专项 fixture 从 20/20 增至 **24/24 PASS**；v2 抽核为 **19/20**。

### 5.2 逐年证据

| 公告年 | 样本 | 通过 | 结果 |
|---:|---:|---:|---|
| 2017 | 1 | 1 | PASS |
| 2018 | 1 | 1 | PASS |
| 2019 | 3 | 2 | PARTIAL |
| 2020 | 3 | 3 | PASS |
| 2021 | 3 | 3 | PASS |
| 2022 | 3 | 3 | PASS |
| 2023 | 3 | 3 | PASS |
| 2024 | 3 | 3 | PASS |
| **合计** | **20** | **19** | **PARTIAL** |

唯一失败：

| 证券 | 财年 | 公告日 | Tushare | 巨潮原件 | 判定 |
|---|---|---|---:|---:|---|
| `603259.SH` | 2018 | 2019-06-19 | `0.58002/股` | `0.58/股`（`5.80/10股`） | 精确不一致，fail-closed |

不把该差异解释为“可忽略舍入”；是否允许容差会改变事件值及边界，当前人裁没有授权。

## 六、B2 可证明起点菜单

### B2-P1：`2021-01-01`

- 研究区间候选：`2021-01-01 <= current_initial_ann_date < 2024-07-01`。
- 证据链：公告年 2020–2024 抽核 `15/15`；作为第一年事件的 2021 年记录，其上年基线来自公告年 2020，故不消费 2019 年失败值。
- 对应财年 2019–2023 的严格初始覆盖均高于 99.8%。
- 冻结清洗前机械候选参考：5,055（up 2,253 / down 2,802）。该数只作 NFV 规划参考，不是正式事件硬断言。
- 代价：研究期缩短，无法回答 2011–2020 的登记时间范围；公告抽核仍是分层样本，不是 26,467 组逐组人工确认。

### B2-STOP：不设起点，停止 exp19

若 John 不接受 2021 起点或认为抽核证据不足，则停止 exp19；不得自行选择更早起点、另设金额容差、更换公告锚或以实施值回填。

### 为什么不列其他起点

- `2020-01-01` 会消费 2019 年的上年基线证据，而 2019 抽核仍有 `603259.SH` 精确不一致。
- `2019-01-01` 的上年基线财年 2017 仅 1/2,791 组可识别，无法形成全市场年度变化口径。
- 2022/2023/2024 等更晚起点没有新增证据优势，只会增加选择自由度并缩短样本，故不列。

## 七、状态、边界与下一步

### 当前状态

- exp19=`registered`；`frozen_at/result_json/done_at` 均空。
- 台账 26 行：registered 8 / frozen 2 / done 14 / closed 2。
- 草案 JSON 未改、仍 NOT-FROZEN；草案 digest 不得冻结。
- 零收益读取、零终版 PAP、零冻结、零研究 manifest、零正式运行、零 persist。
- exp18 继续停在原语义硬门。

### 下一步只允许人裁

John 明确选择：

1. `B2-P1`：研究期起点=`2021-01-01`，随后另令终版 PAP；或
2. `B2-STOP`：停止 exp19。

本单元在此停交验点。

## 八、验证与证据锚

### 代码与运行

- 正式画像修复：`581565b`（`dict_row` 按命名列读取；已推 GitHub，阿里云 HEAD 同步）。
- `verify_dividend` v2：`24/24 PASS`。
- 运行镜像：`shuheng-quant:579a354`，image ID=`sha256:e7b9b27078353243490c557c103cfeda95b139ebd11dfea3a31b606ede6ebfc3`；当前代码只读挂载。
- 新镜像构建仅在 Docker Hub metadata 获取阶段超时，发生于任何正式数据动作之前，未生成新镜像；失败日志保留。
- 可移植 HTML 报告通过 artifact schema、打包与结构验证；本机无兼容 Chromium，因此验证级别为 `structural_only`，语义回退表完整，但未做浏览器像素/交互验收。
- 数据库只读套件最终复跑：StudySnapshot probes `19/19`、snapshot mirror `11/11`、manifest lineage `24/24`、integration `7/7`，全部通过。
- 最终只读回报（`2026-08-06 11:57:45+08`）：exp19 仍 `registered` 三槽空，台账仍 `8/2/14/2` 共 26 行；snapshot 375 权威行、镜像与 publication attestation 三处一致，且 taosha 半键数为 0，确属源级快照。
- 内部证据包递归 `SHA256SUMS` 共 154 项，逐项校验全部通过；传输前既有 13 类秘密扫描 `TOTAL_HITS=0`，原始响应与公告 PDF 未修改、未公开再分发。

### 主要 SHA256

| 实物 | SHA256 |
|---|---|
| 原始响应 JSONL | `68358800128d641ab8cdc8f921a902c2edd6c732501d30578f25f71014021ceb` |
| 拉取 manifest | `d0264d8e9e88bddbc500943b28dd65f2fa1ce0d178631207a51ab3a6de8ccbb8` |
| 全量画像 v1 | `010d7d2790b334416dff7e681250f9b63333d4db311131351e2172257c5533c5` |
| 独立污染 SQL 对账 | `981558863773a8867de827ddf87ccb5e854657993d8d9aeabe69b14218e9ea0d` |
| 公告抽核 v1 | `bc9a0e533c2dfd28375004a11ebf2cd2065dff5e9a2014ba2bea1ef547ed0451` |
| 公告抽核 v2 | `90953e0f24212b69b4d8049c586b536caa32df5da55b987508af2ba107b00bf2` |
| 源级快照 digest | `2df8e289a7c1dbacebf571db100d36d4786e4af2b994018a795882a7d4c7a6b7` |

内部证据目录：阿里云 `/root/s19dataclose/`。原始响应与公告 PDF 不进入 Git、不公开再分发。
