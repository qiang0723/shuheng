# exp8 冻结前回修二次执行令(人 2026-07-17,原文即口径,F 条留痕)

> 性质:外部复核结论 = exp8 冻结前回修交付**暂不通过**,**撤回原冻结批复**,退回冻结前回修。
> 本档为人令逐字留痕;工地不得善意改写。执行档/验收档另立。
>
> 效力登记(承本令§一):
> - **原冻结批复(2026-07-17,绑定 PAP digest `a6a2da9aeaf72ab8479b49b06b0206b86d4bbe0305f064e6da5b2d66f07393a9`)已撤回**;
> - **原 PAP digest `a6a2da9a…` 已作废**(文件本体永久保留不动,另立撤回标记档);
> - 作废原因 = C1、C3、C6 及 P1-4 未按裁定形成真实执行行为;
> - 原测试虽为绿灯,但部分 fixture 固化的是旧错误行为,**不构成验收通过**(原验收档 `limit-open-premend-acceptance-2026-07-17.md` 头部已标撤回);
> - 原"负向/中低把握 55%"预判同步作废,只绑定原 digest,不得复制/平移/自动继承到新 PAP。

---

以下为人令原文,逐字:

---

**枢衡工地:**

外部复核结论:exp8冻结前回修交付暂不通过,撤回原冻结批复,退回冻结前回修。以下为完整执行令。

## 一、前置:撤回原冻结批复

撤回2026-07-17对exp8的冻结批复。

原批复绑定的PAP:

- 文件:`taosha/docs/limit-open-pap-final-2026-07-17.json`
- SHA256:`a6a2da9aeaf72ab8479b49b06b0206b86d4bbe0305f064e6da5b2d66f07393a9`

经外部复核发现四项冻结阻断,现明确作废。不得再据此进行driver施工、manifest生成、正式运行或台账终态写入。

原PAP、原冻结批复和原digest记录不得删除、覆盖或静默改写。须在新裁决档及`ops/STATE.md`中显式登记:

- 原冻结批复已撤回;
- 原PAP digest已作废;
- 作废原因是C1、C3、C6及P1-4未按裁定形成真实执行行为;
- 原测试虽为绿灯,但部分fixture固化的是旧错误行为,不构成验收通过。

原"负向/中低把握55%"预判同步作废。该预判只绑定原PAP digest,不得复制、平移或自动继承到新PAP。新PAP通过后,由人重新签发冻结句并重新封入预判;即使新预判文字相同,也必须形成与新digest一一绑定的新记录。

## 二、开工前状态闸

第一动作只读检查exp8数据库实物,回报:

- 当前status;
- 当前PAP digest或冻结锚;
- `frozen_at`;
- 是否已经执行过冻结迁移;
- 是否已经存在driver、manifest或正式运行记录;
- 台账总行数及exp8 result是否为空。

按以下分支执行:

1. 若exp8仍为`registered`且未真正冻结:保持`registered`,进入本令后续回修。
2. 若exp8已经为`frozen`或更后状态:立即停止,不得旁路SQL解冻、回写`registered`、删除时间戳或自行关闭实验;只提交上述只读实物,等待人另下合法状态处置令。
3. 若已生成manifest、读取收益或发生正式运行:立即停止并完整报告,不得继续本令施工。

## 三、已确认通过、不得扩大返工的部分

以下修法保留,不重新设计:

- C2:`st_policy='keep'`为exp8显式值,既有默认仍为`reject`;
- C4:重复映射涉事链全部fail-closed剔除并逐条上报;
- C5:PIT listing锚定、缺失list_date、上市前bar及上市区间异常fail-closed;
- P1-1:`verdict_policy='adj_bmp_main_only'`,辅助方法不得改变顶层判决;
- 事件链N=2、最大饱和链、反向一字板顺延、唯一事件、研究期及holdout等既有已裁口径。

除非本轮修法造成直接回归,不得重开上述设计。

## 四、冻结阻断一:C1停牌顺延未实现

人令要求:从T+1开始,一字板和停牌均属于不可交易状态,统一顺延,最多5个交易日。

当前代码仍在顺延循环之前把T或T+1停牌直接判为`suspension`并返回;当前fixture还把"T+1纯停牌直接剔除"写成期望值。因此原`32/32 PASS`证明的是旧行为稳定,不是新裁定实现。

修复要求:

- exp8路径中,从T+1起遇到纯停牌、一字板或二者混合,均进入同一顺延计数;
- 顺延1日:保留,τ0移至下一可交易日;
- 顺延5日:保留,属于上限;
- 顺延6日:以`postpone`剔除;
- T+1纯停牌不得再被前置`suspension`分支截断;
- exp8事件日T由真实交易行生成;若仍出现T不可定位或异常停牌状态,须fail-closed并单独留痕,不得与T+1顺延混淆;
- 既有实验默认停牌处置保持逐字节不变,须以显式参数启用exp8新路径。

必须新增可区分fixture:

- 纯停牌连续1日;
- 纯停牌连续5日;
- 纯停牌连续6日;
- 一字板与停牌混合合计5日;
- 一字板与停牌混合合计6日。

不得用混合场景替代纯停牌场景。

## 五、冻结阻断二:C3不得预判"结构性无CAR"

原PAP中的下列表述作废:

> recent_listing层因覆盖门槛112/160结构性无CAR,主结论适用域以seasoned为主。

新口径必须是:

- `recent_listing`与`seasoned`一律应用同一估计窗及112/160覆盖门槛;
- recent_listing存在存活样本时,按冻结方法如实计算并报告;
- 无存活样本时,输出结构化状态
  `UNESTIMABLE_BY_FROZEN_COVERAGE`;
- 同时输出该层事件总数、各阶段存活数、逐因及逐年剔除分解;
- 不得在正式运行前宣告该层必然无CAR;
- 不得预先把主结论适用域限定为seasoned;
- 即使recent_listing最终存活数为0,该诊断块也必须存在,不得因只按存活事件分组而静默缺席。

必须新增两类fixture:

1. recent_listing存在覆盖合格存活样本:输出真实统计块;
2. recent_listing零存活:输出`UNESTIMABLE_BY_FROZEN_COVERAGE`及完整剔除分解。

## 六、冻结阻断三:P1-4必须改变正式报告行为

当前只是把新`bias_statement`写入PAP,但`report.py`仍无条件输出旧固定文案:

- 所有剔除均缩小异常收益;
- 显著结果是真实效应的保守下界;
- 不显著可能只是被保守处置削弱。

上述表述对exp8全部禁用。

修复要求:

- exp8 result必须携带冻结PAP中的偏差声明及其来源锚;
- exp8报告直接消费并渲染该声明;
- exp8固定口径为:

> 清洗剔除与listing fail-closed产生样本选择,偏差方向未知;估计对象仅限清洗存活样本,不得外推为全体一字涨停链事件的效应。

- exp8报告不得出现"保守处置""倾向缩小效应""真实效应不小于报告值""保守下界"等旧结论;
- 既有实验默认报告文案及既有输出必须保持逐字节不变;
- 新旧行为须显式参数化,禁止根据实验编号写隐式分支。

验收须提供:

- exp8报告中旧禁止措辞零命中;
- exp8报告与PAP偏差声明逐字对账;
- 默认旧路径报告逐字节零回归。

## 七、冻结阻断四:C6必须形成真实的非判决诊断结构

当前实现仍复用forecast专属的"预喜/预亏/扭亏"分层文案,并把层内`verdict`改名为`sig_state_report_only`。改名不等于取消判决,属于字面合规、实质旁路。

exp8要求两个正交诊断维度:

1. `recent_listing / seasoned`
2. `ST / non-ST`

修复要求:

- 两个维度必须分别、明确、结构化输出;
- 不得复用"预喜/预亏/扭亏"、正负漂移抵消等forecast专属文案;
- 每个诊断层可以报告:
  - 事件数;
  - 存活数;
  - 剔除数及原因;
  - CAAR;
  - ADJ-BMP及其他预注册报告统计;
- 任何诊断层均不得输出:
  - `verdict`;
  - `verdict_note`;
  - `sig_state_report_only`;
  - 或其他等价的SIG/NOT_SIG/AMBIGUOUS分类字段;
- 所有诊断层在JSON和报告中必须带结构化`NOT_FOR_VERDICT`标记;
- `ST/non-ST`不得只藏在板块计数中,须形成明确的正交诊断维度;
- recent/seasoned和ST/non-ST不产生独立判决,也不改变顶层判决。

另外,顶层主窗内同时存在权威ADJ-BMP与非权威朴素t、BMP等字段。非权威字段必须能够被结构化识别为`NOT_FOR_VERDICT`,不能只在文档末尾附一份块名清单。可拆分字段或增加明确的字段级角色元数据,但不得改变既有默认输出。

验收须递归证明:

- 全result只有顶层一个`verdict`;
- 非权威块中不存在任何改名后的等价判决字段;
- 所有非权威统计均有明确角色标记;
- 报告每个诊断段均明确标记`NOT_FOR_VERDICT`。

## 八、新PAP与留痕要求

不得覆盖原PAP文件。新PAP须使用新的版本文件名,原`a6a2da9a…`文件永久保留并标记已撤回。

交付:

- 新PAP JSON;
- 新文件SHA256;
- canonical内容digest;
- PAP校验输出;
- 原PAP→新PAP逐键diff;
- 新PAP中不得再出现已经作废的C1、C3或偏差方向表述;
- 新裁决档须明确旧digest作废、新digest待批;
- `ops/STATE.md`须显式作废旧冻结批复、旧预判和旧验收结论,禁止新旧结论并存。

新PAP交验通过前,不得自行写入新的方向或把握度预判;预判只能由人随新冻结批复写入。

## 九、重新交验清单

至少提交:

1. 开工前数据库状态闸只读实物;
2. 新PAP JSON、SHA256、canonical digest及逐键diff;
3. C1纯停牌1/5/6日及混合边界fixture;
4. C3 recent_listing有存活与零存活两类fixture;
5. exp8报告旧"保守下界"措辞零命中及新声明逐字对账;
6. recent/seasoned与ST/non-ST两个正交诊断维度的JSON和报告实物;
7. 递归证明顶层唯一verdict、诊断层无等价判决字段;
8. 非权威字段结构化角色检查;
9. `limit_open_rules`、engine专项测试及全家福回归;
10. 既有默认路径和既有实验输出逐字节零回归证明;
11. 完整`git diff --stat`、`git diff --name-status`、实际diff及commit链;
12. 未写driver、未读收益、未生成manifest、未写台账、未正式运行的边界证明。

## 十、范围禁令

本轮仍为冻结前回修,不构成冻结令。

允许触碰范围仅限:

- 新版PAP及裁决/验收留痕;
- exp8生成器设计;
- `limit_open_rules.py`及专项fixture;
- 最小范围的`cleaning.py`、`survivors.py`、`runner.py`、`report.py`;
- `ops/STATE.md`改判留痕。

禁止:

- 写driver;
- 读取任何收益、CAR、显著性或正式研究结果;
- 生成或发布manifest;
- 冻结exp8;
- 写实验台账;
- 正式运行;
- 修改qbase事实源或既有StudySnapshot;
- 重开C2、C4、C5及`adj_bmp_main_only`设计;
- 借本轮新增通用分层平台或无关重构。

完成后停在验收点,等待人重新审核新PAP。只有人以新digest重新下达冻结句并重新写入方向与把握度预判后,方可进入driver施工、manifest和正式运行。
