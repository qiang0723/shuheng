# Q3-B 行情重灌 · 完整性核对（2026-07-07）

> 人裁 A（分页全票重灌，修 tushare `daily`/`adj_factor` 单票 6000 行硬顶截断）执行条件②③的验收留痕。
> **库实物为唯一真身**；本文数字均来自 `sudo -u postgres psql qbase` 现场查询。

## 0. 背景

阻塞根因（Q3 增补①完整性核对发现）：tushare `daily`/`adj_factor` 接口单票**硬顶 6000 行**，长历史票早年数据被截断（非停牌缺行）。例：`000001.SZ` 旧批仅起 2001-02-16、缺 1991–2001。
人裁 **A**：游标式向后分页全票重灌（B 转永久技术债、C 行数识别不可靠均否）。三条执行：①旧批标废不删、视图取 max batch 路由新批；②完整性核对全量重跑、逐票闭合、分页拼接不重不漏；③adj_factor 一并重灌同验。

采集件 = `qbase/ingest/seed_marketdata.py`（分页版 commit `eb401a9`）。运行 aliyun-new，log `/tmp/q3b-rebackfill.log`，打印 `REBACKFILL_ALL_DONE` 收尾。

## 1. 新批实物（append-only，旧批保留标废）

| 源 | 旧批（截断，保留） | 新批（重灌全史） | 增量 |
|---|---|---|---|
| `tushare:daily` → `bar_daily_snap` | batch3 = 17,138,664 | **batch6 = 17,943,344** | +804,680 |
| `tushare:adj_factor` → `adj_factor_snap` | batch4 = 17,680,484 | **batch7 = 18,762,389** | +1,081,905 |
| `tushare:trade_cal` → `trade_cal_snap` | batch5 = 13,162（未截断，不重灌） | — | — |

seed 自检：落库行数 == 源拉行数 == 流式写行数，去双发/分页重叠 = 0（单事务末尾一次性 COMMIT）。

## 2. 不重（唯一性）

| 表·批 | 行数 | distinct(ts_code,trade_date) | 结论 |
|---|---|---|---|
| bar_daily_snap b6 | 17,943,344 | 17,943,344 | **零重复 ✅** |
| adj_factor_snap b7 | 18,762,389 | 18,762,389 | **零重复 ✅** |

分页拼接重叠由逐片整行去重兜住，全表 (ts_code,trade_date) 无残留重复。

## 3. 6000 顶已破 + 逐票闭合（不漏）

「被截断票」精确判据 = 旧批中**恰好 6000 行**的 ts_code（硬顶填满者，真实史 ≥6000）。逐票查新批行数：

| 源 | 旧批 ==6000（被截票） | 新批 >6000 | 新批 ==6000 | 新批 <6000（可疑） | 新批行数 min/max |
|---|---|---|---|---|---|
| daily | 883 | **883** | 0 | **0** | 6002 / 8545 |
| adj_factor | 1010 | **1010** | 0 | **0** | 6009 / 8676 |

**daily 883/883、adj 1010/1010 全部 >6000，零可疑残留** → 截断之外的早年数据全部补回，逐票闭合。
新批全表恰好 6000 行的票 = 0（daily/adj 均是），6000 硬顶彻底破除。

### 923 数字改判（诚实留痕）
STATE 早前记「923 只长史票卡 6000」= 分页 dry-run 阶段的**估计数**，非库精确核定。重灌后按库实物精确核定：**daily 截断 883 只 / adj_factor 截断 1010 只**（两源并集 1011 只）。旧「923」作废，以此为准。差异归因 = dry-run 估计口径 ≠ 落库后按 batch 精确 GROUP BY 计数。

## 4. 早年数据补回（视图端抽验）

`explore_reader_prices`（经视图，含 holdout `< 2024-07-01`）：

| ts_code | min trade_date | max trade_date | 行数 | 判据 |
|---|---|---|---|---|
| 000001.SZ 平安银行 | **1991-04-04** | 2024-06-28 | 7897 | 旧批截至 2001-02-16 → 全史回来 ✅ |
| 600000.SH 浦发银行 | 1999-11-10 | 2024-06-28 | 5851 | 上市即 1999，正常 |

底表 `bar_daily_snap` b6 最早交易日 = **1990-12-19**（A 股开市日），旧批起 2001。holdout WHERE 生效（两票 max 均 < 2024-07-01）。

## 5. 视图路由到新批（人裁 A 执行条件①）

`explore_reader_prices` 视图 join 条件按 `max(batch_id) WHERE source=...` 动态路由：
- daily → `max batch = 6`、adj_factor → `max batch = 7`（现场查 fact_batch 确认）。
- 新批 `fact_batch.note` 已写【重灌·游标分页全史】+ `supersedes 旧截断批（daily=batch3/adj=batch4）` + 废弃原因。旧批 append-only 保留（fact_batch note 不可改，废弃说明写在新批 note）。
- 票集一致性：daily b6 票集 ⊆ adj b7 票集（差集 = 0）。覆盖票 5860 = 宇宙 5861 − T600018.SH（退市 2006 无 tushare 行情，对齐 Q3-B 验收档）。

## 6. 结论

重灌完整性核对**全部闭合**：不重（§2）/ 不漏·逐票闭合（§3）/ 早年补回（§4）/ 视图路由新批（§5）。6000 硬顶截断已彻底消解，Q3 explore_reader 价格半边底料就绪。

**剩余 Q3**：两份规则文档（limit_status 分位取整推导 / is_st PIT 源=entity_alias）+ Q3 完整验收包（008/009 三视图+角色四件套 + 两文档）→ 交人审 → 过则切片3 开工令。

## 7. 北交所排除负面断言（体系原则 2026-07-07，裁 A：视图层 WHERE `ts_code !~ '\.BJ$'`）

人裁「北交所全体系不做」，排除口径 A = explore_reader 视图 DDL 层加 `!~ '\.BJ$'`（同 holdout 焊法，非应用层）；底表忠实存全 328 只 .BJ 不动（宇宙完整、防幸存偏差、可追溯）。识别口径 = `.BJ` 后缀（920 段 325 只实证在案，数字段口径「8开头/43/83/87」作废——漏 920 段）。

**负面断言（库实物，经视图查，期望全 0）**：

| 视图 | 经视图查 .BJ 计数 | 判据 |
|---|---|---|
| explore_reader_prices | **0** | ✅ 北交所排除 + 泄漏测试过 |
| explore_reader_events | **0** | ✅ |
| explore_reader_calendar | 不适用 | 交易日轴无 ts_code 标的维度 |

**排除量留档（体检报告口径注记：事件宇宙=沪深，北交所按体系原则排除）**：

| 侧 | 排除 .BJ | 备注 |
|---|---|---|
| prices（价格） | 252 票 / 196,807 行（holdout 内有 bar 者；底表 .BJ 全体 328） | 196,807 = board=bse 行数，闭环一致 |
| events（forecast 事件） | 53 票（合格事件：first_ann 非空且 <holdout） | forecast .BJ 全体 187 票 |
| holdertrade（供口径注记，不入本视图） | 220 票 | — |

**排除后视图规模（沪深）**：prices 5363 票 / events 105,584 事件。板块分层维持四层（主板/创业板/科创板/ST）无北交所层。
