# Q2 · 巨潮采集件 cninfo.py 借入 + 覆盖度自查(2026-07-07)

> STATE 记录。落位事实 + 覆盖度自查结论。**只自查、不改造**;缺口停下另议,不自行扩写(人 2026-07-07 令)。

## 一、借入落位(已完成)

| 项 | 值 |
|---|---|
| 源仓 | `github.com/quant-newman/radar`(我方私有 org,已确权,见 ops 核对报告) |
| 源文件 | `src/radar/cninfo.py` @ HEAD `7d6f7f13`(本体 HEAD==origin HEAD) |
| sha256 | `7875485ceeb23f496bac6bf4550d0a7776e3af3603445df8b11dfd53670a4fde` |
| 落位 | `qbase/ingest/cninfo.py`,逐字 copy(落位后 sha256 复核一致),176 行,零内部 import |
| commit | `4c144552` |
| 雷达本体 | **一行未动** |

## 二、采集件做什么(客观)

`fetch_announcements(code, start, end, category="")` = 抓单票某窗口公告**列表**,不入库、不解读、不抽事件、不打标签(文件红线)。
- `category=""` 默认 = **全量**(不按类目过滤)。
- 每条返回**列表元数据**:`announcement_id` / `stock_code` / `raw_company_name` / **`title`** / `valid_time`(announcementTime→UTC,双时态精度保住) / `announcement_type` / `source_url`(PDF 链接)。
- **不下载、不解析 PDF 正文。**

## 三、覆盖度自查:是否含减持预披露(#3 事件源所需)?

**结论:部分覆盖。事件日锚点可达;对账所需结构化减持要素不覆盖。**

| 维度 | 判定 | 证据 |
|---|---|---|
| 事件存在性 + 事件日 | ✅ 可达 | 减持预披露公告属公开公告,在 `category=""` 全量抓取范围内被捕获;`title`/`announcement_type` 供下游识别,`valid_time`(公告时刻 UTC)可作事件日,与 #4 forecast 锚 first_ann_date 同粒度 |
| 针对性识别 | ⚠ 靠下游 | 本刀不打标签、无"减持预披露"巨潮类目代码/标题过滤;识别哪条是减持预披露由**下游**按 title/type 做。本刀只保证"抓到",不保证"标出" |
| 结构化减持要素 | ❌ **不覆盖** | 拟减持股东 / 数量 / 比例 / 期间 / 方式 等在**公告 PDF 正文**;本刀只给 `source_url` 链接,**不下载不解析**。列表元数据里没有这些字段 |

## 四、缺口与影响(停下,补采方案另议)

- **若 #3 只需"减持预披露事件发生 + 事件日"作事件锚点** → 本刀**够用**(下游按 title/type 识别 + valid_time 锚事件日)。
- **若 #3 需拟减持规模/比例作事件强度,或与 `holdertrade_snap` 事后实施按「ts_code+股东+窗口」对账**(见 `caveats-and-ledger.md` C2 关系声明)→ **缺口**:股东/窗口/规模在 PDF 正文,本刀不解析,**需补正文解析件**。此为**另议项,不自行扩写**。
- **待验证(采集时,非阻塞)**:`category=""` 全量是否稳定返回减持预披露公告,或该类目需显式 category 码——静态代码无法证,须实采抽验。

**待人拍**:#3 到底需要哪一档——仅事件日锚点(本刀够),还是含结构化减持要素(需另议补正文解析)?据此定巨潮腿后续。

## 五、裁定(人 2026-07-07):档位二

**依据**:PAP #3 冻结定义含"**减持比例 ≥ 总股本 1%**"门槛,须从预披露公告 **PIT 读取**,不得用 holdertrade 事后数据反筛(前视)。故 PDF 正文解析件 = **真需求**,立为**施工清单增补项**(挂账 L4→见 `caveats-and-ledger.md` L3)。
- **范围钉死**:仅减持预披露类公告;仅抽 3 字段=**股东名 / 拟减持比例上限 / 减持期间**;解析失败行**如实标注**;**不做通用解析框架**。
- **开工排序**:**v1.5 已批复(2026-07-07)入清单**;开工 = 切片 2 验收**之后**,**此前不动工**。
- `cninfo.py` 本刀(抓列表元数据+事件日锚)维持不动、已可用;解析件是其下游补件,非改造本刀。
