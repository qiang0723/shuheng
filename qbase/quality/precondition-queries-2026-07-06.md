# 两个前置查询 · 2026-07-06

> 施工清单 v0.3 / 设计方案 §9 · Q2 开跑双前置之一。老库只读(SELECT-only,未写老平台)。

## 前置查询 #1 · 行情三问(老 `marketdata` 库,`md` schema)

**结论:三问全部 YES。行情库完整,`日线行情回填子项`(§9 应急)不需激活。**

| 问 | 答 | 证据 |
|---|---|---|
| **全市场?** | ✅ 是 | `bar_daily_raw` 5,863 只、`security` 5,862 只;三所齐全:深 SZSE 3,078 / 沪 SSE 2,456 / 北 BSE 328 |
| **含退市?** | ✅ 是 | `security.list_status`:上市 L=5,527、退市 D=335;`delist_date` 齐;335 只退市中 **333 只在 `bar_daily_raw` 有行情** → 无幸存者偏差 |
| **复权因子?** | ✅ 是 | `md.adj_factor` 5,867 只 · 1990-12-19 → 2026-07-06 · 1,757 万行 |
| **交易日历?** | ✅ 是 | `md.trade_calendar` SSE 13,162 天 + SZSE 12,966 天 · 1990-12-19 → **2026-12-31**(含未来日历) |

深度:主表 `bar_daily_raw`(1,782 万行)、`daily_basic`(1,563 万)、`moneyflow`(1,315 万)、`margin_detail`(657 万)自 **1990-12-19** 起。另有 `forecast`(766)、`holder_trade`(1,131)等——量小,Q2 回填时需核是否全史(留待 Q2)。

## 前置查询 #2 · 雷达股池:append 还是覆盖?

**直接结论:结构上是 append 历史(可 PIT 重建),不是覆盖式。** 但**深度只有 6 周且全部在 holdout 线之后**——这一点需你裁决,见下。

**证据:**

| 对象 | 性质 | 时间跨度 | 判定 |
|---|---|---|---|
| `radar.heat_signal`(热度信号,A7 口径基底) | append,带 `trade_date`+`valid_time`+`observed_time` 双时戳 | **2026-05-20 → 2026-07-03**,32 个交易日、53 个采集批次、3,590 行 | append 历史 ✅ |
| `research_view.ref_membership_snap`(成员快照) | append,带 `snap_date` | **2026-07-03 → 2026-07-05**,3 个快照、665 行 | append 历史,极短 |
| `radar.entities`(当前观察宇宙) | 无日期列,151 只 | 仅当前态 | 覆盖式 |

**关键含义(需你裁决):**

- 三段时间线:探索区 `< 2024-07-01` / holdout `2024-07-01 起` / 实盘验证区 `上线后`。
- 雷达热度/股池数据**最早只到 2026-05-20**——**探索区(<2024-07-01)零雷达数据**,现有 6 周全落在 holdout 区。
- 因此 **假设#1(radar_heat)、假设#2(drawdown_rebuy 用雷达股池)在探索区没有可用样本**:雷达当年还没在采集。设计 §9 对股池的 (a)「append 存在即照跑」判据,**默认了历史深度**;此处结构满足 (a) 但深度不成立。

**我的建议(产出是报告,不擅动方案,请你拍):**

1. **#1 / #2**:探索区无雷达样本 → 按 §9 精神,二者要么记「数据前提暂不成立」挂起,等雷达在实盘验证区自然积累;要么 #2 走 §9 路径 (b) 改**确定性规则池**(公共事实定义、任意历史日可用 `marketdata` 重算,探索区/holdout 全可回溯),family 继承、门槛抬高。
2. 无论哪条,**「雷达股池」的精确口径**(= 当前 `entities` 宇宙?还是每日 `heat_signal` 触发集?)属假设#2 冻结 PAP 参数,我不擅定,请你明确。
3. 建议**切片3 首条端到端仍用假设#4(业绩预告)**——它吃 `marketdata.forecast`/qbase 回填快照,历史深、不依赖雷达,数据前提干净。这也与 spec §7「切片3 建议 #4」一致。

— 待人裁 —
