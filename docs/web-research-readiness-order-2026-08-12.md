# Web 研究就绪度可读性增量令

时间口径：2026-08-12，Asia/Shanghai（UTC+8）。

## 一、授权来源与限缩解释

John 已在 Web 侧明确裁定“web侧等能优化的时候继续优化”，并在 exp22 跨期暂停、研究主线无可直接施工入口后回复：

> 继续下一步任务吧

本单元据此只做 Web 静态页面的可读性增量：直接回答当前能否输出个股，并展示 exp18/21/23/22 的证据停点。不得把该回复扩解为实时选股、在线数据、荐股、研究恢复或公开部署授权。

## 二、数据身份

1. 已闭卷研究数字继续只消费 `docs/web-snapshot-2026-08-09/`，不重算、不改值；
2. 研发停点只消费仓内 `ops/STATE.md` 一百五十一笔及对应闭合文档；
3. 页面必须把“研究结果快照时点”与“研发状态时点”分开显示，禁止合并成一个伪最新快照；
4. 当前没有经过冻结与验证的实时个股候选生成器，页面须明确显示“不输出个股候选”，不得用实验事件样本冒充当前股票推荐。

## 三、允许触碰面

- Web 首页、专属静态组件、fixture、样式与 SSR 测试；
- 本令、交付档与 `ops/STATE.md`；
- 若本机 Docker 服务仍在，允许在新镜像通过全部门后替换同一 loopback 私有服务。

禁止修改研究结果 fixture 数值、实验状态、研究代码、SQL、数据库、在线 API、登录、域名、反向代理、公开访问策略或推荐股票列表。

## 四、验收

须通过：lint、生产构建、SSR 断言、规模闸门、架构闸门；Docker 替换仅在镜像构建内上述门全部通过后进行。页面须在桌面与窄屏结构上保持可读，不新增横向滚动依赖。

完成即停交验点，不自动推送。
