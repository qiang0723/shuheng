# Web 研究停点可追溯增量令

时间口径：2026-08-12，Asia/Shanghai（UTC+8）。

## 一、授权来源与限缩解释

John 在 Web 研究就绪度可读性增量闭合后回复：

> 继续下一个任务

当前研究主线中，exp18/21/23 均停证据硬门，exp22 已跨期暂停；不得把本回复扩解为恢复任何实验。下一项只做 Web 静态可追溯增量：主页四张研究停点卡可进入对应实验详情，详情页明确显示当前硬门、原因及研发状态时点。

## 二、数据身份

1. 研究结果、台账生命周期与校准数字继续只消费 `docs/web-snapshot-2026-08-09/`，不重算、不改值；
2. exp18/21/23/22 的停点文字继续只消费 `ops/STATE.md` 一百五十一笔及已闭合文档；
3. 详情页必须区分研究结果快照与研发状态快照，不得把后续状态说明写成新的统计结果；
4. 停点说明只解释为什么尚不能继续，不提供代理证据、替代锚、候选股票或恢复建议。

## 三、允许触碰面

- `ResearchReadiness` 主页停点卡；
- 实验详情页、静态 fixture helper、专属样式与 SSR 测试；
- 本令、交付档与 `ops/STATE.md`；
- 全部门通过后，允许替换既有本地 loopback Docker 服务。

禁止数据库、在线 API、登录、研究代码、SQL、实验状态、收益、股票列表、公开部署与 Sites 托管。

## 四、验收与停止线

须通过 lint、production build、SSR、规模闸门与架构闸门；HTTP 读回须证明四张卡均可追溯到详情，exp22 详情仍显示“跨期暂停”，并保留两类独立时点。

完成即停交验点，不自动推送。
