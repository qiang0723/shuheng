# Web 静态 MVP · 2026-08-09 快照更新与私有部署令

- 日期：2026-08-09（UTC+8 / Asia/Shanghai）
- 授权人：John
- 执行形态：F-first 留痕 → 只读快照 → 静态更新 → 验证 → 私有部署 → 停交验点

## 人令原文（逐字）

> 批准 Web 静态 MVP 进入 2026-08-09 快照更新与私有部署单元。只读提取当前权威台账、闭卷 result 与校准册，更新仓内静态 fixture、页面数字、状态和来源时点；完成 lint、生产构建、SSR 与浏览器显示复核后私有部署。零数据库写入、零在线 API、零登录、零研究运行，完成停交验点。

## 授权范围

1. 使用只读数据库会话提取当前权威实验台账、已闭卷 result 与校准册，形成仓内可复核静态快照。
2. 只更新 `web/` 静态 fixture、页面数字、实验状态、来源文件和 UTC+8 快照时点；不接数据库、不新增在线 API、不新增登录。
3. 执行源码 lint、生产构建、SSR 测试、代码规模闸门和浏览器显示复核。
4. 验证通过后，以本次经验证并推送的精确源码创建或更新 Sites 项目，执行私有部署并核验部署状态与页面显示。
5. 生成交付档并更新 `ops/STATE.md`，完成即停交验点。

## 硬边界

- 数据库全程只读，禁止任何写入、状态迁移、manifest、addendum 或 persist。
- 不运行任何研究、收益读取、recon、敏感性分析或实验统计路径。
- Web 继续保持静态架构；部署不授权 API、登录、定时同步或后台任务。
- exp18、exp21、exp23 的证据语义硬门维持原状；exp1、exp6 冻结状态维持原状。
- 任何快照数字须可追溯到本次只读提取件；不凭会话记忆补数。
- 私有部署不可用时停止报人，不得自动改为公开部署。

## 验收点

- 仓内静态快照与页面 fixture 逐项对账；
- lint、生产构建、SSR、规模闸门全部通过；
- 浏览器首页、实验列表、至少两个详情页和窄屏显示通过；
- 私有部署成功且部署页面读回与本次快照一致；
- Git 工作树、部署版本和交付档锚定同一源码状态。
