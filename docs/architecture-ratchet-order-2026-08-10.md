# 枢衡架构依赖棘轮施工令

- 日期：2026-08-10（UTC+8 / Asia/Shanghai）
- 性质：代码规模闸门之后的最小架构治理；不构成研究功能或大重构授权

## 一、人令原文与唯一解释

John 原文：

> 好，按照你的计划来

上文被批准的计划是：控制横向复制；新实验优先配置化和复用公共组件；存量热点只在自然
触碰时拆分并用回归保护；不做一次性大重构。因此本单元只新增机械依赖棘轮和说明文档，
不拆 `runner.py`、`report.py` 或任何既有 driver，不移动模块，不修改实验行为。

## 二、授权面

1. 只读扫描 `ops/qbase/taosha` Python 模块的内部 import 图；
2. 机械拒绝新增循环依赖、底层模块反向依赖 harness，以及新的实验专属 rules/driver
   横向依赖；
3. 对当前仅有的实验专属横向依赖建立精确基线，实行“新增拒绝、减少后必须下调基线”；
4. 将架构闸门接入 Docker 构建，并提供隔离 self-test 与全仓实扫命令；
5. 形成验收档并更新 `ops/STATE.md`。

## 三、硬边界

- 零数据库连接与写入，零数据采集、缓存、PAP、冻结、StudySnapshot、manifest、收益读取、
  实验运行或 persist；
- 零研究逻辑、统计内核、reader、driver、fixture 和 Web 业务页面改动；
- 不增加平台能力、公共模块或配置系统；本轮只立防线，不借治理名义重构；
- 存量例外只能按当前实物精确登记，不得预留额度；例外消失后必须同步删除。

## 四、完成条件

隔离正反向 self-test、全仓依赖扫描、既有代码规模闸门均通过；Dockerfile 在构建期强制执行
两道闸门；触碰面严格限于 `ops/` 治理件、Dockerfile、README、验收档与 STATE。完成即停。
