# 判别力补测·外部复核第二回件与时序补测令(2026-07-13,人转达原文留痕)

> 本档为裁决留痕:外部第二独立视角复核对判别力补测(§11)的回件 + 时序补测指令,逐字收录,先于施工 commit(F 条)。

## 复核结论原文

- #1:闭合。
- #3:暂未完全闭合。
- #2/#4/#5:未发现回归。
- 超范围改动:未发现;84f77c7..a678f7d仅有裁决留痕、验收档和STATE,无生产代码修改。
- diff口径:更正为18文件、+1327/−96,通过。
- 硬化窗口:暂不能关闭。

唯一阻塞点:水印批11/12均在主读取查询开始之后才提交。受PostgreSQL单条查询MVCC快照语义影响,查询开始时快照已固定,即便主读错误走current/max路径,该查询也可能仍然只看到查询开始时刻已可见的旧批次(9/10),而旧批9/10与批8内容相同——因此"主读产出仍等于批8"这个结果,在"主读走对了snapshot 38"和"主读走错了但因MVCC时序原因看不到水印、蒙对了"这两种情况下都可能成立,判别力歧义并未消除,只是从"内容层面"转移到了"时序层面"重新出现。

后置SQL独立复算证明的是复算路径钉住了快照,与主读取路径本身走的是哪条路无关,不能替代对主读取这一步的时序攻击。

要求(继续仅限隔离环境,零生产代码修改,只调整测试编排):

必须在"source snapshot 38及anchor已经读取完成"之后、且在主数据SELECT或服务器游标开始之前,提交可区分水印批。随后再启动主读取,证明产出仍来自批8。验收须提供四个有序时间点,并满足严格不等式:

- T_anchor(source snapshot/anchor读取完成)
- T_watermark_commit(水印批提交完成)
- T_main_select_start(主读取SELECT或服务器游标开始)
- T_finish(seed产出完成)

必须满足:T_anchor < T_watermark_commit < T_main_select_start < T_finish。

只有这个时序关系成立,才能保证:如果主读走错误的current/max路径,该查询的MVCC快照必然能看到水印(因为水印在查询快照固化前已提交);而正确的snapshot路径仍然只看到批8。此时"产出仍等于批8"才是真正具有判别力的证据,不再有"蒙对"的可能。

## 我方接令注记(施工前)

- **认同复核点**:①READ COMMITTED 单语句快照在语句开始时固化,主读=单条流式大查询(seed 代码实证:锚读→"源快照绑定"→计数查询→"逐票聚合中"→主读游标);②我方 §11.3 实测时间线主读游标开启(≈21:32:01.91)早于水印 commit(21:32:02.066),严格先后无法证明;③SQL 复算是独立后起语句,钉不住主读语句的路由。
- 修正=纯编排时序调整:水印触发点从"逐票聚合中"提前到**"源快照绑定"行(锚读取完成)**;主数据 SELECT 在计数查询之后(实测余量 mret≈88s / pret≈49s)才开始,四时点严格不等式秒级余量。
- `T_main_select_start` 双源:主证=`pg_stat_activity.query_start`(服务器端时钟,轮询捕获主读语句);辅证="逐票聚合中"/"Path A"打印时戳(主读开启的时间下界)。全部时钟同宿主机(seed/PG/编排同机)。
- 加强项(白捡):前轮水印批 11/12 已存 iso 库且=max(trade_cal)——本轮 seed 启动起**任意相位任意语句**(含锚读、诊断计数查询)走 current/max 皆必见可区分内容;诊断计数查询虽横跨/先于新水印,其产出贡献=0(仅 view_rows 元数据),且被此项覆盖。计数值与前轮打印值全等另作断言。
- 范围:仅调整 /root/r3disc/ 编排件(orchestrate2.py 新件,orchestrate.py 原件留档);iso 两库/水印设计/证据集/隔离约束(零生产代码改动、零生产库写入)全部不变。
