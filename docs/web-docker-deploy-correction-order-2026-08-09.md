# Web 静态 MVP · Docker 部署口径补正令

- 日期：2026-08-09（UTC+8 / Asia/Shanghai）
- 授权人：John
- 性质：部署机制补正，不改变 2026-08-09 权威静态快照与页面内容

## 人令原文（逐字）

> 部署使用docker部署

## 执行解释与边界

1. 2026-08-09 Web 静态快照、页面 fixture 与显示验收继续有效；仅将正式部署机制由 Sites 改为 Docker。
2. 既有 Sites 私有 v1 不再作为正式部署结论；其仍保持 owner-only，不自动公开，也不在无删除授权时扩大处置。
3. 因人令未指定远端主机，本单元采用最小权限默认：部署到 John 当前 Mac 的本机 Docker，引出端口仅绑定 `127.0.0.1`，不开放局域网或公网。
4. 新增 Web 专属生产 Dockerfile、构建上下文忽略文件与 Compose 服务；根量化镜像职责与默认行为不改。
5. 镜像须使用与 `web/package.json` 一致的 Node 24.14.0 钉死基础镜像与 pnpm 11.9.0，构建阶段执行 lint、生产 build 与 SSR；运行阶段非 root、只读文件系统、最小 tmpfs、健康检查。
6. 完成镜像构建、Compose 启动、容器内外 HTTP、浏览器桌面与窄屏显示复核后停交验点。

## 禁区

- 零数据库写入、零在线 API、零登录功能、零研究运行；
- 不改权威快照数字、实验状态、判决读法或研究代码；
- 不开放 `0.0.0.0`、不配置公网反向代理、域名或证书；
- 若本机 Docker 不可用或生产容器无法只读运行，停止报人，不改为其他部署机制。
