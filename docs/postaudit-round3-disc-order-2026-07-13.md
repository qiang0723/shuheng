# 第三轮窄补·外部复核回件与判别力补测令(2026-07-13,人转达原文留痕)

> 本档为裁决留痕:人转达外部第二独立视角复核意见 + 补测指令原文,逐字收录,先于施工 commit(F 条)。

## 人令原文

硬化窗口当前状态:#1/#2/#4/#5均已闭合,#3为唯一未闭合项,是当前关窗的唯一阻塞点。

外部第二独立视角复核指出:此前#3的并发验证测试缺乏判别力——并发写入的trade_cal批次9/10与锚定批次8业务内容逐行相同,即便seed实际读错了新批次,结果仍会表现为"0/0全等",因此无法证明实际读取来源确实是批次8。

要求(仅限隔离验证环境,不得修改生产代码):

1. seed绑定快照批次8运行期间,并发提交内容可区分的新qbase批次(9/10),或注入等价的可观察水印(部分字段值设为与批次8不同的、可识别标记)。
2. 运行后直接证明:①实际读取的数据内容确实来自批次8,而非批次9/10的可区分内容/水印;②source_anchor记录的确实是批次8。
3. 不接受:相同内容批次对照、静态多次复现、仅检查anchor字段而不交叉验证实际读取的数据内容。

diff口径修正:验收档需更正为完整实物8ea6b12..84f77c7共18文件,+1327/−96(此前16文件+1180/−95有误),核实差异来源并更正§10记录。

完成后提交验收实物(含实际读取数据内容的直接证据),附diff --stat更正版,转外部第二独立视角复核。复核通过后报人终签关窗。

## 我方接令注记(施工前)

- 范围:仅两件——①隔离验证环境判别力并发补测(零生产代码改动、零生产库写入);②验收档 §10 diff 口径更正。不夹带。
- 隔离环境操作化:阿里云同机克隆 qbase/taosha 两库为 `qbase_iso`/`taosha_iso`(TEMPLATE 快拷,触发器/角色权限随库保留),seed 以环境变量 DSN 指向 iso 库运行,生产两库零写入;水印批次只存在于 iso 库。
- 水印设计:iso 库内并发提交新 trade_cal 批次,内容与批次 8 可区分且**若被误读必然改变 seed 产出**(翻转真实交易日 is_open 及已知含 bar 的非交易日),证据=产出内容与批次 8 逐日全等、与水印批次可区分内容矛盾 + source_anchor=8 + 水印 commit 时间戳落 seed 运行窗内。
- diff 差异来源已复算钉死:§10 原"16文件+1180/−95"=`8ea6b12..4fc4663` 排除留痕档+STATE 的代码面子口径(可复算全等);"全范围18文件+1270/−96"统计时点在代码终 `4fc4663`,漏计终稿 commit `84f77c7` 自身(+67/−10,自引用盲区);完整实物 `8ea6b12..84f77c7`=**18文件+1327/−96**,与外审数一致。
