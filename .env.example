# 秘钥纪律:所有 token / DSN 只住 .env,不进代码、不进 git。
# 每台机器持有自己的 .env;下面按机器分区。复制为 .env 后填真值。

# ── AWS 工地 (.env on AWS) ────────────────────────────────
# AWS 只做服务与施工,对 qbase 一律只读,永不持有写 DSN。
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 结果副本只读 DSN(阿里云 role_service,归一/台账上线后填):
# QBASE_SERVICE_DSN_RO=postgresql://role_service:***@127.0.0.1:5433/qbase   # 经只读隧道

# ── 阿里云本体 (.env on Aliyun,绝不出现在 AWS) ───────────
# TUSHARE_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx   # tushare 采集
# QBASE_DSN=postgresql://qbase_app:***@127.0.0.1:5432/qbase    # qbase 写库(本地)
# 老库只读账号(同云内网,SELECT-only;坩埚/观澜不直连):
# OLD_MARKETDATA_DSN_RO=postgresql://ro:***@<old>:5432/marketdata
# OLD_RADAR_DSN_RO=postgresql://ro:***@<old>:5432/radar
# OLD_RESEARCH_DSN_RO=postgresql://ro:***@<old>:5432/research
